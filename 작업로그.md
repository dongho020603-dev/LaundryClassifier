# 세탁 기호 분류 앱 개발 작업로그

**프로젝트명:** LaundryClassifier
**개발 환경:** React Native 0.80.1 (CLI)
**목표:** On-Device AI 기반 YOLOv8n 세탁 기호 분류 앱 개발

---

## 📌 채팅 재개 시 사용할 명령어

```
작업로그.md 읽고 지금까지 한 작업 전부 자세하게 업데이트해줘.
파일 변경사항, 패키지 설치/제거, 설정 수정 등 모든 걸 단계별로 기록하고,
논문 쓸 때 참고할 수 있게 누가 봐도 이해할 수 있게 작성해줘.
채팅 끝날 때마다 이 작업로그를 최신화하는 거 잊지 마.
```

---

## 📱 0단계: React Native 프로젝트 초기 구성 (2026-04-14 ~ 04-20)

### 0.1 개발 환경 구축 (2026-04-14)

**작업자:** 서동호

앱 개발을 위한 로컬 개발 환경을 구축하였다.

**설치 항목:**

| 항목 | 버전 | 용도 |
|------|------|------|
| Node.js | 20.18.3 (LTS) | JavaScript 런타임 |
| JDK | 17.0.18.8 (Eclipse Adoptium) | Android 빌드용 |
| Android Studio | Ladybug | Android SDK, 에뮬레이터 관리 |
| Android SDK | API 35 (Android 15) | compileSdk, targetSdk |
| Android SDK Build-Tools | 35.0.0 | APK 빌드 |
| VS Code | 최신 | 코드 에디터 |

**Android SDK 설정:**
- `compileSdkVersion`: 35
- `targetSdkVersion`: 35
- `minSdkVersion`: 24 (Android 7.0 이상)
- `buildToolsVersion`: 35.0.0
- `ndkVersion`: 프로젝트 기본값

**환경 변수:**
```powershell
JAVA_HOME = 'C:\Program Files\Eclipse Adoptium\jdk-17.0.18.8-hotspot'
ANDROID_HOME = '%LOCALAPPDATA%\Android\Sdk'
```

---

### 0.2 React Native 프로젝트 생성 (2026-04-15)

**작업자:** 서동호

초기에는 Expo 프레임워크를 사용하여 프로젝트를 생성하였다. Expo를 선택한 이유는 초기 프로토타입을 빠르게 구성하기 위함이었다.

**프로젝트 생성:**
```bash
npx create-expo-app LaundryClassifier --template blank-typescript
cd LaundryClassifier
```

**초기 프로젝트 구조:**
```
LaundryClassifier/
├── app/                    # Expo Router (file-based routing)
│   └── index.tsx           # 메인 화면
├── android/                # Android 네이티브 코드 (npx expo prebuild)
├── node_modules/
├── app.json                # Expo 설정 파일
├── package.json
└── tsconfig.json
```

**초기 의존성:**
- `expo`: Expo 프레임워크
- `expo-router`: 파일 기반 라우팅
- `expo-splash-screen`: 스플래시 화면
- `expo-camera` (CameraView): 카메라 접근
- `react`: 18.3.1
- `react-native`: Expo가 관리하는 버전

---

### 0.3 Expo 기반 초기 화면 프로토타입 (2026-04-16 ~ 04-17)

**작업자:** 서동호

Expo Router를 사용하여 기본적인 화면 구성을 프로토타이핑하였다.

**구현한 화면:**
1. **메인 화면 (`app/index.tsx`)**: 카메라 촬영 / 갤러리 선택 버튼
2. **결과 화면**: 선택한 이미지와 분석 결과 표시 (더미 데이터)

**사용한 라이브러리:**
- `expo-camera` (CameraView): 카메라 촬영
- `expo-image-picker`: 갤러리에서 이미지 선택

**확인된 한계점:**
- Expo Router의 file-based routing이 단순한 2~3개 화면 앱에는 오버스펙
- `expo-camera`의 CameraView가 카메라 제어 유연성이 부족
- 향후 TFLite 네이티브 모듈 연동 시 Expo의 managed workflow로는 한계가 있음
- KIDURIN 레퍼런스 프로젝트가 순수 React Native CLI를 사용하고 있어 구조 통일 필요

---

### 0.4 Expo → React Native CLI 전환 결정 (2026-04-18)

**작업자:** 서동호

팀 미팅을 통해 Expo에서 순수 React Native CLI로 전환하기로 결정하였다.

**전환 사유:**
1. **네이티브 모듈 필요**: TFLite 모델을 직접 로드하여 추론하기 위해 Kotlin 네이티브 모듈 작성이 필수적이며, Expo managed workflow에서는 이를 지원하지 않음
2. **KIDURIN 레퍼런스 호환**: 참고하는 KIDURIN 프로젝트가 React Native CLI 기반이므로, 동일 구조를 채택하면 코드 참조 및 문제 해결이 용이
3. **Vision Camera 사용**: `react-native-vision-camera`는 `expo-camera`보다 고성능 카메라 제어가 가능하며, 네이티브 프레임 프로세싱을 지원
4. **빌드 제어**: `./gradlew` 직접 사용으로 빌드 과정을 세밀하게 제어 가능

**전환 방식:**
- 기존 Expo 프로젝트에서 `npx expo prebuild`로 생성된 `android/` 폴더를 기반으로, Expo 의존성을 수동 제거하는 방식을 선택
- 새 프로젝트를 처음부터 만드는 것보다, 기존 Gradle 설정과 Android 구조를 유지하면서 Expo 코드만 걷어내는 것이 효율적이라 판단

---

### 0.5 Android 네이티브 코드 생성 (2026-04-19)

**작업자:** 서동호

Expo prebuild를 사용하여 Android 네이티브 코드를 생성하였다.

```bash
npx expo prebuild --platform android
```

**생성된 주요 파일:**
- `android/app/src/main/java/com/anonymous/LaundryClassifier/MainActivity.kt`
- `android/app/src/main/java/com/anonymous/LaundryClassifier/MainApplication.kt`
- `android/app/src/main/AndroidManifest.xml`
- `android/app/src/main/res/values/styles.xml`
- `android/app/build.gradle`
- `android/build.gradle`
- `android/settings.gradle`
- `android/gradle/wrapper/gradle-wrapper.properties` (Gradle 8.11.1)

**문제 확인:**
- `MainActivity.kt`에 `expo.modules.splashscreen.SplashScreenManager` import 존재
- `MainApplication.kt`에 `expo.modules.ReactNativeHostWrapper` import 존재
- `getJSMainModuleName()`이 `.expo/.virtual-metro-entry` 반환 (Expo 전용 entry point)
- `getMainComponentName()`이 `"main"` 반환 (index.js의 AppRegistry 등록명 `"laundryclassifier"`와 불일치)
- `AndroidManifest.xml`에 Expo Updates 관련 메타데이터 존재
- `styles.xml`에 Expo SplashScreen 테마 정의 존재

→ 이 상태에서는 앱이 정상 실행되지 않음. Expo 의존성 제거 작업 필요.

---

## 📋 작업 개요 - Expo 제거 및 CLI 전환 (2026-04-29)

### 변환 전 상태
- **문제점:**
  - Expo Router로 구성된 앱 (file-based routing)
  - `MainActivity.kt`, `MainApplication.kt`에 Expo 의존성 코드 남아있음
  - `AndroidManifest.xml`에 Expo 메타데이터 존재
  - `MainApplication.kt`의 `getJSMainModuleName()`이 `.expo/.virtual-metro-entry` 반환
  - `MainActivity.kt`의 `getMainComponentName()`이 "main" 반환 (index.js는 "laundryclassifier"로 등록)
  - → **앱이 실행되지 않는 주요 원인**

### 변환 후 목표
- 순수 React Native CLI 구조 (KIDURIN 참고)
- Metro bundler만 사용 (`npm start`)
- `./gradlew app:installDebug`로 간단히 디버깅
- 카메라/갤러리로 사진 입력 → YOLOv8 분류 (향후 통합)

---

## 🛠️ 1단계: Expo 완전 제거

### 1.1 MainActivity.kt 변경
**파일:** `android/app/src/main/java/com/anonymous/LaundryClassifier/MainActivity.kt`

**Before:**
```kotlin
import expo.modules.splashscreen.SplashScreenManager
import expo.modules.ReactActivityDelegateWrapper

override fun getMainComponentName(): String = "main"
```

**After:**
```kotlin
// Expo import 제거
override fun getMainComponentName(): String = "laundryclassifier"  // ✅ index.js와 일치

override fun createReactActivityDelegate(): ReactActivityDelegate {
    return DefaultReactActivityDelegate(this, mainComponentName, fabricEnabled)
}
```

---

### 1.2 MainApplication.kt 변경
**파일:** `android/app/src/main/java/com/anonymous/LaundryClassifier/MainApplication.kt`

**Before:**
```kotlin
import expo.modules.ReactNativeHostWrapper
import expo.modules.ApplicationLifecycleDispatcher

override fun getJSMainModuleName(): String = ".expo/.virtual-metro-entry"  // ❌
```

**After:**
```kotlin
// Expo import 제거

override fun getJSMainModuleName(): String = "index"  // ✅ index.js 직접 로드

override val reactHost: ReactHost
  get() = DefaultNewArchitectureEntryPoint.createReactHost(applicationContext, reactNativeHost)
```

---

### 1.3 AndroidManifest.xml 정리
**파일:** `android/app/src/main/AndroidManifest.xml`

**제거된 내용:**
```xml
<!-- 삭제됨 -->
<meta-data android:name="expo.modules.updates.ENABLED" android:value="false"/>
<meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="ALWAYS"/>
<meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0"/>
```

---

### 1.4 app.json 최소화
**파일:** `app.json`

**Before:** Expo 플러그인 설정 (expo-router, expo-splash-screen 등)

**After:**
```json
{
  "name": "LaundryClassifier",
  "displayName": "LaundryClassifier"
}
```

---

## 🏗️ 2단계: 앱 구조 재구성

### 2.1 새로운 폴더 구조 (KIDURIN 스타일)

```
LaundryClassifier/
├── android/                 # 안드로이드 네이티브 코드
│   └── app/src/main/
│       ├── assets/models/best_int8.tflite  # YOLOv8n INT8 모델
│       └── java/.../
│           ├── LaundryYOLOModule.kt        # TFLite 추론 네이티브 모듈
│           └── LaundryYOLOPackage.kt       # 패키지 등록
├── src/
│   ├── data/                    # ✅ 05-10 추가
│   │   ├── laundrySymbolData.ts     # 38개 기호 데이터 + 코스 매핑
│   │   └── laundryLogic.ts          # 조합 로직, 멘트 결정, 경고, 추천
│   ├── screens/
│   │   ├── HomeScreen.tsx           # 메인 화면 (카메라/갤러리 선택)
│   │   ├── CameraScreen.tsx         # Vision Camera 1:1 촬영
│   │   ├── ModelDebugScreen.tsx     # YOLO 추론 + bbox 시각화
│   │   └── ResultScreen.tsx         # 세탁 가이드 결과 (요약+상세)
│   ├── services/
│   │   ├── NativeLaundryYOLO.ts     # 네이티브 모듈 JS Bridge
│   │   └── imageProcessingService.ts # 640x640 리사이징
│   ├── components/              # (향후 공통 컴포넌트)
│   ├── utils/                   # (향후 유틸 함수)
│   └── App.tsx                  # 메인 앱 컴포넌트 (4화면 전환)
├── index.js                 # Entry point
└── package.json
```

### 2.2 index.js 수정
**Before:**
```javascript
import App from './app/index';  // Expo Router 경로
```

**After:**
```javascript
import App from './src/App';  // src/App.tsx로 변경
```

---

## 📦 3단계: 필요 라이브러리 설치

### 3.1 이미지 피커 설치
```bash
npm install react-native-image-picker
```

**이미 설치된 라이브러리:**
- `react-native-vision-camera` 4.6.3 (카메라 - 향후 사용 가능)
- `react-native-gesture-handler`
- `react-native-reanimated`
- `react-native-safe-area-context`
- `react-native-screens`

---

## 🎨 4단계: 화면 구현

### 4.1 HomeScreen.tsx
**경로:** `src/screens/HomeScreen.tsx`

**기능:**
- 두 개의 버튼: "📷 카메라로 촬영", "🖼️ 갤러리에서 선택"
- `react-native-image-picker` 사용
- 이미지 선택 시 `onImageSelected(uri)` 콜백 호출

**특징:**
- 간단한 UI (녹색 카메라 버튼, 파란색 갤러리 버튼)
- 에러 핸들링 포함

---

### 4.2 ResultScreen.tsx ✅ 05-10 전면 재작성
**경로:** `src/screens/ResultScreen.tsx`

**초기 버전 (04-29):** 플레이스홀더 — 이미지 표시 + "TFLite 모델 연결 후 가이드가 표시됩니다" 텍스트

**현재 버전 (05-10):** 세탁 가이드 결과창 v3 — 2화면 구조(요약→상세)
- 화면 1: 메인 멘트 + 4칸 카테고리 카드 + 전문케어 배너
- 화면 2: 항목 카드 리스트 + 주의 조합 경고 + 추천 세탁 순서
- YOLO 추론 결과(detections)를 받아 `laundryLogic.ts`로 분석
- 상세 구현 내용은 아래 "결과 화면 구현 (05-10)" 섹션 참조

---

### 4.3 App.tsx ✅ 05-10 데이터 전달 경로 수정
**경로:** `src/App.tsx`

**기능:**
- 4개 화면 전환 로직 (home → camera → modelDebug → result)
- 상태 관리: `currentScreen`, `selectedImageUri`, `resizedImageUri`, `detections`
- detections 배열을 ModelDebugScreen → ResultScreen으로 전달

**플로우:**
```
[HomeScreen] → 카메라 촬영 / 갤러리 선택
    ↓
[CameraScreen] → 사진 촬영
    ↓
[ModelDebugScreen] → YOLO 추론 + bbox 시각화
    ↓ detections 전달
[ResultScreen] → 세탁 가이드 결과 (요약 ↔ 상세)
```

---

## ⚙️ 5단계: 실행 방법

### 5.1 Metro 번들러 시작
**터미널 1:**
```powershell
cd C:\Users\tjehd\capstone\LaundryClassifier
npm start
```

**기대 출력:**
```
Welcome to React Native v0.81
Starting dev server on http://localhost:8081
INFO  Dev server ready.
```

---

### 5.2 앱 빌드 및 설치
**터미널 2:**
```powershell
cd C:\Users\tjehd\capstone\LaundryClassifier\android

# JAVA_HOME 설정 (본인 환경에 맞게 수정 필요)
$env:JAVA_HOME = 'C:\Program Files\Eclipse Adoptium\jdk-17.0.18.8-hotspot'

./gradlew app:installDebug
```

**주의사항:**
- 현재 JAVA_HOME 경로가 `jdk-17.0.15.6-hotspot`일 가능성 있음
- `java -version` 명령어로 실제 JDK 경로 확인 후 설정

---

### 5.3 USB 디버깅 확인
```powershell
adb devices
```

**출력 예시:**
```
List of devices attached
<device_id>    device
```

---

## 🔧 주요 변경 사항 요약

| 항목 | Before | After |
|------|--------|-------|
| **앱 구조** | Expo Router (file-based) | 순수 React Native (src/) |
| **Entry Point** | `.expo/.virtual-metro-entry` | `index.js` → `src/App.tsx` |
| **MainActivity** | Expo 의존성 포함 | 순수 React Native |
| **MainApplication** | Expo 의존성 포함 | 순수 React Native |
| **카메라** | Expo Camera (CameraView) | react-native-image-picker |
| **내비게이션** | expo-router | 조건부 렌더링 (간단) |
| **실시간 자동촬영** | 구현 예정 (KIDURIN 참고) | ❌ 제외 (수동 촬영만) |

---

## 🚀 다음 단계 (TODO) - 04-29 시점 작성, 05-10에 대부분 완료

### 1. YOLOv8 모델 통합 ✅ (05-10 완료)
- [x] `yolov8n.pt` 모델을 TFLite로 변환 → `best_int8.tflite`
- [x] Kotlin 네이티브 모듈로 직접 구현 (`LaundryYOLOModule.kt`)
- [x] `ModelDebugScreen.tsx`에서 실제 추론 및 시각화 구현

### 2. 이미지 전처리 ✅ (05-10 완료)
- [x] YOLO 입력 사이즈에 맞게 리사이징 (640x640) → `imageProcessingService.ts`
- [x] 정규화 처리 (0~255 → 0~1) → `LaundryYOLOModule.kt`의 `bitmapToTensor()`
- [x] Tensor 변환 (Bitmap → ByteBuffer, NHWC 포맷)

### 3. 결과 후처리 ✅ (05-10 완료)
- [x] Bounding box 그리기 → `ModelDebugScreen.tsx` bbox 오버레이
- [x] 신뢰도 필터링 (threshold: 0.25) + NMS (IoU: 0.45)
- [x] 한글 라벨 매핑 → `laundrySymbolData.ts` (38개 기호 전체, v3 사용자 어투)

### 4. UI/UX 개선 (일부 완료)
- [x] 로딩 애니메이션 → `ModelDebugScreen.tsx` ActivityIndicator
- [x] 결과 화면에 이미지 위에 박스 표시 → bbox 오버레이 구현
- [ ] 다크 모드 지원

### 5. 성능 최적화
- [x] 모델 추론 속도 측정 → `inferenceTime` 표시
- [ ] 이미지 크기 최적화
- [ ] 메모리 사용량 모니터링

---

## 🔨 6단계: 빌드 문제 해결 및 앱 설치 (2026-04-29 세션 2)

### 6.1 SplashScreen 리소스 오류 해결

**문제:**
```
error: resource style/Theme.SplashScreen not found
error: style attribute 'attr/windowSplashScreenBackground' not found
```

**원인:**
- `android/app/src/main/res/values/styles.xml`에 Expo SplashScreen 테마가 남아있음
- Expo 패키지는 제거했지만 리소스 정의는 그대로 존재

**해결:**
**파일:** `android/app/src/main/res/values/styles.xml`

**삭제된 코드:**
```xml
<style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
  <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
  <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen_logo</item>
  <item name="postSplashScreenTheme">@style/AppTheme</item>
  <item name="android:windowSplashScreenBehavior">icon_preferred</item>
</style>
```

**명령어:**
```bash
./gradlew clean  # 빌드 캐시 정리
```

---

### 6.2 MainApplication.kt 컴파일 오류 해결

**문제:**
```
Unresolved reference 'createReactHost'
Unresolved reference 'REACT_NATIVE_RELEASE_LEVEL'
```

**원인:**
- React Native 0.81.5에서 사용 불가능한 API 사용
- New Architecture 관련 코드가 제대로 설정되지 않음

**해결:**
**파일:** `android/app/src/main/java/com/anonymous/LaundryClassifier/MainApplication.kt`

**Before:**
```kotlin
override val reactHost: ReactHost
  get() = DefaultNewArchitectureEntryPoint.createReactHost(applicationContext, reactNativeHost)

override fun onCreate() {
  super.onCreate()
  DefaultNewArchitectureEntryPoint.releaseLevel = try {
    ReleaseLevel.valueOf(BuildConfig.REACT_NATIVE_RELEASE_LEVEL.uppercase())
  } catch (e: IllegalArgumentException) {
    ReleaseLevel.STABLE
  }
  loadReactNative(this)
}
```

**After:**
```kotlin
override val reactHost: ReactHost? = null

override fun onCreate() {
  super.onCreate()
  SoLoader.init(this, false)
  if (BuildConfig.IS_NEW_ARCHITECTURE_ENABLED) {
    load()
  }
}
```

**추가된 import:**
```kotlin
import com.facebook.react.defaults.DefaultNewArchitectureEntryPoint.load
import com.facebook.soloader.SoLoader
```

**변경된 ReactNativeHost:**
```kotlin
override val isHermesEnabled: Boolean = BuildConfig.IS_HERMES_ENABLED
```

---

### 6.3 BuildConfig 필드 추가

**파일:** `android/app/build.gradle`

**추가된 내용:**

1. **buildFeatures 활성화:**
```gradle
android {
    namespace 'com.anonymous.LaundryClassifier'

    buildFeatures {
        buildConfig = true
    }
}
```

2. **BuildConfig 필드 정의:**
```gradle
defaultConfig {
    applicationId 'com.anonymous.LaundryClassifier'
    minSdkVersion rootProject.ext.minSdkVersion
    targetSdkVersion rootProject.ext.targetSdkVersion
    versionCode 1
    versionName "1.0.0"
    buildConfigField("boolean", "IS_NEW_ARCHITECTURE_ENABLED", "false")
    buildConfigField("boolean", "IS_HERMES_ENABLED", enableHermes.toString())
}
```

---

### 6.4 react-native-image-picker 링킹 문제 해결

**문제:**
```
error: package com.imagepicker does not exist
import com.imagepicker.ImagePickerPackage;
```

**원인:**
- Autolinking이 PackageList.java는 생성했지만, 모듈 컴파일은 하지 않음
- `:react-native-image-picker:compileDebugJavaWithJavac` 태스크가 실행되지 않음

**시도한 해결 방법:**
1. ❌ npm 재설치: `npm uninstall react-native-image-picker && npm install react-native-image-picker@8.2.1`
2. ❌ 빌드 캐시 삭제: `rm -rf app/build && ./gradlew clean`
3. ✅ **수동 링킹 (최종 해결책)**

**최종 해결:**

**파일 1:** `android/settings.gradle`

**추가된 코드:**
```gradle
rootProject.name = 'LaundryClassifier'
include ':app'
include ':react-native-image-picker'
project(':react-native-image-picker').projectDir = new File(rootProject.projectDir, '../node_modules/react-native-image-picker/android')

includeBuild('../node_modules/@react-native/gradle-plugin')
```

**파일 2:** `android/app/build.gradle`

**추가된 의존성:**
```gradle
dependencies {
    implementation("com.facebook.react:react-android")
    implementation project(':react-native-image-picker')  // ✅ 수동 추가

    if (enableHermes) {
        implementation("com.facebook.react:hermes-android")
    }
}
```

---

### 6.5 빌드 및 설치 성공

**명령어:**
```bash
cd LaundryClassifier/android
JAVA_HOME='/c/Program Files/Eclipse Adoptium/jdk-17.0.18.8-hotspot' ./gradlew clean
JAVA_HOME='/c/Program Files/Eclipse Adoptium/jdk-17.0.18.8-hotspot' ./gradlew app:installDebug
```

**빌드 로그 (주요 태스크):**
```
> Task :react-native-image-picker:compileDebugJavaWithJavac  ✅ 모듈 컴파일 성공
> Task :app:compileDebugKotlin  ✅ Kotlin 컴파일 성공
> Task :app:compileDebugJavaWithJavac  ✅ Java 컴파일 성공
> Task :app:packageDebug  ✅ APK 패키징 성공
> Task :app:installDebug  ✅ 디바이스 설치 성공

Installing APK 'app-debug.apk' on 'SM-A908N - 12' for :app:debug
Installed on 1 device.

BUILD SUCCESSFUL in 37s
69 actionable tasks: 59 executed, 10 up-to-date
```

**설치된 디바이스:**
- 기기명: SM-A908N (Galaxy A90 5G)
- Android 버전: 12
- 연결 방법: USB 디버깅 (adb)

---

### 6.6 앱 실행 확인

**명령어:**
```bash
# 앱 실행
adb shell am start -n com.anonymous.LaundryClassifier/.MainActivity

# Metro 상태 확인
curl http://localhost:8081/status
# 출력: packager-status:running ✅
```

**실행 결과:**
- ✅ 앱이 디바이스에서 정상 실행됨
- ✅ Metro bundler와 연결됨
- ✅ JavaScript 번들 로드 성공

---

## 📊 빌드 최종 구성 요약

### Android 빌드 설정

| 파일 | 주요 설정 | 값 |
|------|----------|-----|
| `build.gradle` (root) | buildToolsVersion | 35.0.0 |
| | compileSdkVersion | 35 |
| | targetSdkVersion | 35 |
| | minSdkVersion | 24 |
| | kotlinVersion | 2.1.20 |
| `app/build.gradle` | applicationId | com.anonymous.LaundryClassifier |
| | IS_NEW_ARCHITECTURE_ENABLED | false |
| | IS_HERMES_ENABLED | true |
| | buildConfig | enabled |
| `settings.gradle` | 수동 링킹 모듈 | react-native-image-picker |

### 수동 링킹이 필요한 이유 분석

**Autolinking의 한계:**
- React Native CLI의 autolinking은 `PackageList.java` 생성까지만 처리
- 일부 환경에서는 네이티브 모듈의 Gradle 프로젝트가 자동으로 include되지 않음
- 특히 Expo에서 CLI로 변환한 프로젝트에서 발생 가능

**수동 링킹 방식:**
1. `settings.gradle`에 모듈 프로젝트 추가
2. `app/build.gradle`에 의존성 추가
3. PackageList.java는 자동 생성된 것 사용

**향후 개선 방안:**
- React Native 버전 업그레이드 시 autolinking 재확인
- 다른 네이티브 모듈 추가 시 동일한 수동 링킹 필요 가능

---

## 🎯 다음 단계 업데이트

### 완료된 작업 ✅
- [x] Expo 의존성 완전 제거
- [x] styles.xml SplashScreen 테마 제거
- [x] MainApplication.kt New Architecture 호환 코드 수정
- [x] BuildConfig 필드 추가 (IS_NEW_ARCHITECTURE_ENABLED, IS_HERMES_ENABLED)
- [x] react-native-image-picker 수동 링킹
- [x] 앱 빌드 성공 (app-debug.apk)
- [x] 디바이스 설치 성공 (SM-A908N)
- [x] 앱 실행 확인
- [x] Metro bundler 연결 확인

### 다음 작업 예정 🔜

#### 1. 앱 기능 테스트
- [ ] HomeScreen UI 확인 (카메라/갤러리 버튼)
- [ ] 카메라 권한 요청 동작 확인
- [ ] 갤러리 권한 요청 동작 확인 (READ_MEDIA_IMAGES)
- [ ] 이미지 선택 후 ResultScreen 전환 확인
- [ ] 3초 로딩 애니메이션 확인
- [ ] 가짜 분석 결과 표시 확인

#### 2. 권한 처리 개선
- [ ] AndroidManifest.xml 권한 설정 검증
  - `CAMERA` 권한
  - `READ_MEDIA_IMAGES` 권한 (Android 13+)
  - `READ_EXTERNAL_STORAGE` 권한 (Android 12 이하)
- [ ] 런타임 권한 요청 로직 확인
- [ ] 권한 거부 시 사용자 안내 메시지

#### 3. YOLOv8 모델 통합 (본격 작업) ✅ 05-10 완료
- [x] `yolov8n.pt` 모델을 TFLite로 변환 → `best_int8.tflite` (INT8 양자화)
- [x] TFLite 모델 파일을 Android assets에 배치
  - 경로: `android/app/src/main/assets/models/best_int8.tflite`
- [x] Kotlin 네이티브 모듈로 직접 구현 (`LaundryYOLOModule.kt`)
- [x] 모델 로드 및 추론 테스트 완료
- [x] `ModelDebugScreen.tsx`에서 실제 추론 및 bbox 시각화 구현

#### 4. 이미지 전처리 구현 ✅ 05-10 완료
- [x] YOLO 입력 사이즈 리사이징 (640x640) → `imageProcessingService.ts`
- [x] 이미지 정규화 (0-1 범위) → `LaundryYOLOModule.kt`
- [x] RGB 채널 순서 확인 (NHWC 포맷)
- [x] Tensor 변환 (Bitmap → ByteBuffer)

#### 5. 결과 후처리 구현 ✅ 05-10 완료
- [x] Bounding box 좌표 계산 (xywh → x1y1x2y2)
- [x] NMS (Non-Maximum Suppression) 적용 (IoU 0.45)
- [x] 신뢰도 임계값 필터링 (threshold: 0.25)
- [x] 세탁 기호 라벨 한글 매핑 → `laundrySymbolData.ts` (38개 기호, 사용자 친화 어투 v3)
- [x] 결과 시각화 (이미지 위 bbox 오버레이)

---

## 🐛 해결된 문제점

### 1. ~~JAVA_HOME 경로 이슈~~ ✅ 해결됨
**증상:** 이전 세션에서 경로 불일치
**해결:** `JAVA_HOME='/c/Program Files/Eclipse Adoptium/jdk-17.0.18.8-hotspot'` 명시적 설정

### 2. ~~SplashScreen 리소스 오류~~ ✅ 해결됨
**증상:** `Theme.SplashScreen` not found
**해결:** `styles.xml`에서 Expo SplashScreen 테마 제거

### 3. ~~MainApplication.kt 컴파일 오류~~ ✅ 해결됨
**증상:** `createReactHost`, `REACT_NATIVE_RELEASE_LEVEL` Unresolved
**해결:** New Architecture 코드 단순화, BuildConfig 필드 추가

### 4. ~~react-native-image-picker 링킹 실패~~ ✅ 해결됨
**증상:** `com.imagepicker.ImagePickerPackage` not found
**해결:** settings.gradle 및 app/build.gradle에 수동 링킹

---

## 📝 참고 자료 및 학습 내용

### React Native CLI 빌드 시스템
- **Gradle Plugin:** `com.facebook.react.settings`를 통한 autolinking
- **BuildConfig:** Kotlin/Java 코드에서 빌드 설정 접근
- **New Architecture:** React Native 0.68+ 옵션 아키텍처
- **Hermes:** JavaScript 엔진 (기본 활성화)

### 네이티브 모듈 링킹
- **Autolinking:** package.json 기반 자동 링킹 (settings.gradle 자동 생성)
- **Manual Linking:** settings.gradle + app/build.gradle 직접 수정
- **PackageList.java:** 자동 생성되는 React Native 패키지 목록

### Gradle 빌드 프로세스
1. `settings.gradle` → 프로젝트 구조 정의
2. `build.gradle` (root) → 공통 설정, 플러그인
3. `app/build.gradle` → 앱별 설정, 의존성
4. Compile → DEX → Package → Install

---

## ✅ 작업 완료 체크리스트 (04-29 시점, 05-10에 업데이트)

### Expo 제거 단계
- [x] Expo 의존성 완전 제거 (MainActivity, MainApplication, AndroidManifest)
- [x] app.json 최소화
- [x] src 폴더 구조 생성
- [x] styles.xml에서 SplashScreen 테마 제거

### 앱 구현 단계
- [x] HomeScreen 구현 (카메라/갤러리 선택)
- [x] ResultScreen 구현 (분석 결과 표시)
- [x] App.tsx 작성 및 화면 전환 로직
- [x] index.js 수정
- [x] react-native-image-picker 설치 및 링킹

### 빌드 설정 단계
- [x] MainApplication.kt 수정 (New Architecture 호환)
- [x] BuildConfig 필드 추가
- [x] react-native-image-picker 수동 링킹
- [x] Gradle 빌드 성공

### 테스트 단계
- [x] 앱 빌드 성공 (./gradlew app:installDebug)
- [x] 디바이스 설치 성공 (SM-A908N)
- [x] Metro bundler 연결 확인
- [x] 앱 실행 확인
- [x] 실제 디바이스에서 기능 테스트 ✅ (05-10 완료)
- [x] 카메라/갤러리 권한 테스트 ✅ (05-10 완료)
- [x] 이미지 선택 및 화면 전환 테스트 ✅ (05-10 완료)

### 향후 작업 (05-10 시점 업데이트)
- [x] YOLOv8 모델 통합 ✅ (05-10 완료)
- [x] 전처리/후처리 구현 ✅ (05-10 완료)
- [x] 실제 분석 기능 구현 ✅ (05-10 완료)
- [ ] UI/UX 개선
- [ ] 성능 최적화

---

## 🚨 6.7 앱 크래시 문제 해결 및 React Native 버전 변경 (2026-04-29 세션 3)

### 문제 발생: 앱 실행 즉시 크래시

**증상:**
- 앱이 디바이스에 설치는 되지만 실행 즉시 종료됨
- Logcat 에러:
```
java.lang.UnsatisfiedLinkError: dlopen failed: library "libreact_featureflagsjni.so" not found
```

**원인 분석:**
- React Native 0.81.5는 존재하지 않는 버전 (0.80 이후 다음 stable은 0.81.0이 아니라 다른 버전)
- 이전 세션에서 잘못된 버전으로 설정되어 필수 네이티브 라이브러리 누락
- KIDURIN 프로젝트는 React Native 0.80.1 사용 (실제 검증된 버전)

---

### 해결 과정

#### 1단계: KIDURIN GitHub 저장소 클론 및 분석

**명령어:**
```bash
cd C:\Users\tjehd\capstone
git clone https://github.com/REI99-9/KIDURIN.git
```

**분석 결과:**

**파일 1:** `KIDURIN/package.json`
```json
{
  "dependencies": {
    "react": "19.1.0",
    "react-native": "0.80.1",
    "react-native-vision-camera": "^4.6.3",
    "react-native-worklets-core": "^2.1.1"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.3",
    "@babel/runtime": "^7.25.0",
    "@react-native/babel-preset": "0.80.1",
    "@react-native/metro-config": "0.80.1",
    "typescript": "5.0.4"
  }
}
```

**핵심 발견:**
- React 19.1.0 (NOT 18.3.1)
- React Native 0.80.1 (NOT 0.81.5)
- `@react-native/babel-preset`와 `@react-native/metro-config` 버전이 React Native와 일치

**파일 2:** `KIDURIN/android/app/src/main/java/com/dipstickcamera/MainApplication.kt`

```kotlin
package com.dipstickcamera

import android.app.Application
import com.facebook.react.PackageList
import com.facebook.react.ReactApplication
import com.facebook.react.ReactHost
import com.facebook.react.ReactNativeApplicationEntryPoint.loadReactNative
import com.facebook.react.ReactNativeHost
import com.facebook.react.ReactPackage
import com.facebook.react.defaults.DefaultReactHost.getDefaultReactHost
import com.facebook.react.defaults.DefaultReactNativeHost

class MainApplication : Application(), ReactApplication {

  override val reactNativeHost: ReactNativeHost =
      object : DefaultReactNativeHost(this) {
        override fun getPackages(): List<ReactPackage> =
            PackageList(this).packages.apply {
              // Packages that cannot be autolinked yet can be added manually here
            }

        override fun getJSMainModuleName(): String = "index"

        override fun getUseDeveloperSupport(): Boolean = BuildConfig.DEBUG

        override val isNewArchEnabled: Boolean = BuildConfig.IS_NEW_ARCHITECTURE_ENABLED
        override val isHermesEnabled: Boolean = BuildConfig.IS_HERMES_ENABLED
      }

  override val reactHost: ReactHost
    get() = getDefaultReactHost(applicationContext, reactNativeHost)

  override fun onCreate() {
    super.onCreate()
    loadReactNative(this)
  }
}
```

**핵심 차이점 발견:**
- ✅ `override val reactHost: ReactHost get() = getDefaultReactHost(...)` (NOT `null`)
- ✅ `loadReactNative(this)` 사용 (SoLoader 직접 호출 안 함)
- ✅ New Architecture 관련 복잡한 조건문 없음
- ✅ 훨씬 간단하고 명확한 구조

---

#### 2단계: LaundryClassifier 의존성 변경

**파일:** `LaundryClassifier/package.json`

**Before:**
```json
{
  "dependencies": {
    "react": "18.3.1",
    "react-native": "0.81.5",
    "react-native-image-picker": "^8.2.1"
  }
}
```

**After:**
```json
{
  "name": "laundryclassifier",
  "main": "index.js",
  "version": "1.0.0",
  "scripts": {
    "start": "react-native start",
    "android": "react-native run-android"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-native": "0.80.1",
    "react-native-image-picker": "^8.2.1"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.3",
    "@babel/runtime": "^7.25.0",
    "@react-native-community/cli": "latest",
    "@react-native-community/cli-platform-android": "19.0.0",
    "@react-native/babel-preset": "0.80.1",
    "@react-native/metro-config": "0.80.1",
    "typescript": "5.0.4"
  },
  "private": true
}
```

**변경 사항:**
- React: 18.3.1 → 19.1.0
- React Native: 0.81.5 → 0.80.1
- `@react-native-community/cli` 추가 (autolinking 오류 해결)
- `@react-native-community/cli-platform-android` 추가
- babel-preset 및 metro-config 버전 통일

**명령어:**
```bash
cd C:\Users\tjehd\capstone\LaundryClassifier
rm -rf node_modules package-lock.json
npm install
```

---

#### 3단계: MainApplication.kt 완전 재작성

**파일:** `android/app/src/main/java/com/anonymous/LaundryClassifier/MainApplication.kt`

**Before (크래시 발생 버전):**
```kotlin
package com.anonymous.LaundryClassifier

import android.app.Application
import com.facebook.react.PackageList
import com.facebook.react.ReactApplication
import com.facebook.react.ReactHost
import com.facebook.react.ReactNativeHost
import com.facebook.react.ReactPackage
import com.facebook.react.defaults.DefaultReactNativeHost
import com.facebook.react.defaults.DefaultNewArchitectureEntryPoint.load
import com.facebook.soloader.SoLoader

class MainApplication : Application(), ReactApplication {

  override val reactNativeHost: ReactNativeHost =
      object : DefaultReactNativeHost(this) {
        override fun getPackages(): List<ReactPackage> =
            PackageList(this).packages.apply {
              // Packages that cannot be autolinked yet can be added manually here
            }

        override fun getJSMainModuleName(): String = "index"

        override fun getUseDeveloperSupport(): Boolean = BuildConfig.DEBUG

        override val isNewArchEnabled: Boolean = BuildConfig.IS_NEW_ARCHITECTURE_ENABLED
        override val isHermesEnabled: Boolean = BuildConfig.IS_HERMES_ENABLED
      }

  override val reactHost: ReactHost? = null  // ❌ null로 설정

  override fun onCreate() {
    super.onCreate()
    SoLoader.init(this, false)  // ❌ 수동 SoLoader 호출
    if (BuildConfig.IS_NEW_ARCHITECTURE_ENABLED) {
      load()
    }
  }
}
```

**After (KIDURIN 패턴 적용 - 정상 동작 버전):**
```kotlin
package com.anonymous.LaundryClassifier

import android.app.Application
import com.facebook.react.PackageList
import com.facebook.react.ReactApplication
import com.facebook.react.ReactHost
import com.facebook.react.ReactNativeApplicationEntryPoint.loadReactNative
import com.facebook.react.ReactNativeHost
import com.facebook.react.ReactPackage
import com.facebook.react.defaults.DefaultReactHost.getDefaultReactHost
import com.facebook.react.defaults.DefaultReactNativeHost

class MainApplication : Application(), ReactApplication {

  override val reactNativeHost: ReactNativeHost =
      object : DefaultReactNativeHost(this) {
        override fun getPackages(): List<ReactPackage> =
            PackageList(this).packages.apply {
              // Packages that cannot be autolinked yet can be added manually here
            }

        override fun getJSMainModuleName(): String = "index"

        override fun getUseDeveloperSupport(): Boolean = BuildConfig.DEBUG

        override val isNewArchEnabled: Boolean = BuildConfig.IS_NEW_ARCHITECTURE_ENABLED
        override val isHermesEnabled: Boolean = BuildConfig.IS_HERMES_ENABLED
      }

  override val reactHost: ReactHost
    get() = getDefaultReactHost(applicationContext, reactNativeHost)  // ✅ 실제 ReactHost 반환

  override fun onCreate() {
    super.onCreate()
    loadReactNative(this)  // ✅ React Native 0.80 표준 초기화
  }
}
```

**핵심 변경사항:**
1. ❌ `override val reactHost: ReactHost? = null`
   ✅ `override val reactHost: ReactHost get() = getDefaultReactHost(applicationContext, reactNativeHost)`

2. ❌ `SoLoader.init(this, false)` 수동 호출
   ✅ `loadReactNative(this)` 사용 (내부에서 자동으로 SoLoader 초기화)

3. ❌ `if (BuildConfig.IS_NEW_ARCHITECTURE_ENABLED) { load() }` 조건부 로딩
   ✅ `loadReactNative(this)`가 BuildConfig에 따라 자동 처리

**왜 이 변경이 크래시를 해결했는가:**
- React Native 0.80.1에서는 `getDefaultReactHost()`가 네이티브 라이브러리 로딩을 올바르게 처리
- `loadReactNative()`가 `libreact_featureflagsjni.so` 등의 JNI 라이브러리를 올바른 순서로 로드
- 수동 SoLoader 초기화는 일부 라이브러리를 누락시킴

---

#### 4단계: babel.config.js 정리

**문제:**
- Metro bundler가 `Cannot find module 'react-native-reanimated/plugin'` 에러 발생
- 이전 세션에서 `react-native-reanimated` 패키지는 제거했으나 babel 설정은 남아있음

**파일:** `babel.config.js`

**Before:**
```javascript
module.exports = {
  presets: ['module:@react-native/babel-preset'],
  plugins: ['react-native-reanimated/plugin'],  // ❌ 모듈 없음
};
```

**After:**
```javascript
module.exports = {
  presets: ['module:@react-native/babel-preset'],
};
```

---

#### 5단계: 빌드 및 Metro 번들러 문제 해결

**5-1. Gradle 빌드 재실행**

```bash
cd android
JAVA_HOME='/c/Program Files/Eclipse Adoptium/jdk-17.0.18.8-hotspot' ./gradlew clean
JAVA_HOME='/c/Program Files/Eclipse Adoptium/jdk-17.0.18.8-hotspot' ./gradlew app:installDebug
```

**결과:**
```
BUILD SUCCESSFUL in 28s
Installing APK 'app-debug.apk' on 'SM-A908N - 12'
Installed on 1 device.
```

**5-2. Metro 번들러 시작 실패 (포트 충돌)**

**문제:**
```bash
npm start

[stderr] error listen EADDRINUSE: address already in use :::8081
Error: listen EADDRINUSE: address already in use :::8081
```

**원인:** 이전 Metro 프로세스가 여전히 8081 포트 사용 중

**해결:**
```powershell
# 1. 포트 사용 중인 프로세스 찾기
netstat -ano | findstr :8081
# 출력: TCP 0.0.0.0:8081 ... LISTENING 55660

# 2. 프로세스 강제 종료
powershell -Command "Stop-Process -Id 55660 -Force"

# 3. Metro 재시작
npm start
```

**성공 출력:**
```
Welcome to React Native v0.80
Starting dev server on http://localhost:8081

INFO  Dev server ready.
```

**5-3. 디바이스 연결 실패 (포트 포워딩 누락)**

**문제:**
- 앱 실행 시 빨간 화면 표시
- "Unable to load script from assets 'index.android.bundle'"
- Metro 로그에 연결 시도 없음

**원인:** ADB 포트 포워딩 미설정

**해결:**
```bash
adb reverse tcp:8081 tcp:8081
```

**설명:**
- 디바이스의 8081 포트를 PC의 8081 포트로 포워딩
- 이제 디바이스에서 `localhost:8081`로 접근하면 PC의 Metro 번들러에 연결됨
- **이 명령은 USB 연결 시마다 매번 실행해야 함** (adb 연결이 끊어지면 포워딩도 사라짐)

**5-4. 최종 성공**

**앱 재시작:**
```bash
adb shell am start -n com.anonymous.LaundryClassifier/.MainActivity
```

**Metro 로그:**
```
INFO  ./index.js ............................ BUNDLE [android, dev] 100.0% (1/1), done.
```

**사용자 확인:** "ㅅㅅ 이제 된다" (Thanks it works!)

---

### 해결 요약

| 문제 | 원인 | 해결 방법 |
|------|------|----------|
| **앱 즉시 크래시** | React Native 0.81.5 (잘못된 버전), libreact_featureflagsjni.so 누락 | React Native 0.80.1로 다운그레이드 |
| **MainApplication 초기화 오류** | `reactHost = null`, 수동 SoLoader 호출 | KIDURIN 패턴 적용: `getDefaultReactHost()`, `loadReactNative()` |
| **Metro 번들링 실패** | babel.config.js에 없는 reanimated 플러그인 참조 | plugins 배열 제거 |
| **Metro 시작 실패** | 이전 프로세스가 8081 포트 점유 | netstat으로 PID 찾아 강제 종료 |
| **디바이스 연결 실패** | ADB 포트 포워딩 미설정 | `adb reverse tcp:8081 tcp:8081` 실행 |
| **Autolinking 오류** | CLI 패키지 누락 | `@react-native-community/cli` 설치 |

---

### 핵심 교훈

#### 1. React Native 버전 호환성의 중요성
- **존재하지 않는 버전 사용 시 심각한 문제 발생**
- 0.81.5는 존재하지 않는 버전이며, 필수 네이티브 라이브러리가 누락됨
- **검증된 레퍼런스 프로젝트(KIDURIN)의 정확한 버전 사용 필수**

#### 2. MainApplication.kt 초기화 패턴
- React Native 0.80+에서는 `loadReactNative()`가 표준 초기화 방법
- 수동 SoLoader 초기화는 라이브러리 로딩 순서 문제 유발 가능
- `getDefaultReactHost()`를 사용해야 ReactHost가 올바르게 초기화됨

#### 3. Metro 번들러 디버깅 체크리스트
1. ✅ Metro가 8081 포트에서 실행 중인가? (`npm start`)
2. ✅ 포트 충돌은 없는가? (`netstat -ano | findstr :8081`)
3. ✅ ADB 포트 포워딩이 설정되었는가? (`adb reverse tcp:8081 tcp:8081`)
4. ✅ babel.config.js가 올바른가? (없는 플러그인 참조 안 하는가?)
5. ✅ 디바이스가 연결되었는가? (`adb devices`)

#### 4. 의존성 관리 원칙
- **package.json 변경 시 반드시 `node_modules` 삭제 후 재설치**
- **babel-preset, metro-config 버전은 React Native 버전과 일치시킬 것**
- **레퍼런스 프로젝트의 의존성을 정확히 따를 것** (버전 추측 금지)

---

## 📱 7. 앱 실행 방법 (상세 가이드)

> **중요:** 이 섹션은 포트 포워딩 등 자주 실수하는 부분을 포함한 완전한 실행 가이드입니다.
> 매번 개발 시작 시 이 순서를 따라야 합니다.

---

### 7.1 사전 준비 (최초 1회)

#### 1) Android 디바이스 준비
1. **USB 디버깅 활성화**
   - 설정 → 휴대전화 정보 → 소프트웨어 정보 → 빌드 번호 7번 탭
   - 설정 → 개발자 옵션 → USB 디버깅 켜기
   - 설정 → 개발자 옵션 → USB를 통한 설치 허용 (선택사항)

2. **USB로 PC 연결**
   - USB 케이블로 연결
   - 디바이스에 "USB 디버깅 허용" 팝업 나오면 **허용** 선택

3. **연결 확인**
   ```powershell
   adb devices
   ```
   **정상 출력:**
   ```
   List of devices attached
   RF8R41PPXXX    device
   ```
   - `unauthorized` 또는 `offline` 나오면 → USB 디버깅 재설정
   - 아무것도 안 나오면 → USB 드라이버 설치 또는 케이블 교체

#### 2) JAVA_HOME 환경변수 확인

**JDK 17 경로 확인:**
```powershell
where java
# 출력 예: C:\Program Files\Eclipse Adoptium\jdk-17.0.18.8-hotspot\bin\java.exe
```

**환경변수 설정 (PowerShell):**
```powershell
$env:JAVA_HOME = 'C:\Program Files\Eclipse Adoptium\jdk-17.0.18.8-hotspot'
```

**영구 설정 (선택사항):**
- 시스템 → 고급 시스템 설정 → 환경 변수
- 시스템 변수에 `JAVA_HOME` 추가
- Path에 `%JAVA_HOME%\bin` 추가

---

### 7.2 일반 개발 워크플로우 (매일 사용)

#### **방법 A: 자동 실행 (권장)**

**단계 1:** 터미널 1개만 열기
```powershell
cd C:\Users\tjehd\capstone\LaundryClassifier
npm run android
```

**설명:**
- Metro 번들러 자동 시작
- 앱 빌드 및 설치
- 자동으로 앱 실행
- **단, 포트 포워딩은 수동으로 해야 함** (아래 참조)

---

#### **방법 B: 수동 실행 (디버깅 시 권장)**

이 방법은 Metro 번들러 로그를 따로 보면서 디버깅할 때 유용합니다.

**단계 1: Metro 번들러 시작**

**터미널 1 (Metro 번들러):**
```powershell
cd C:\Users\tjehd\capstone\LaundryClassifier
npm start
```

**기대 출력:**
```
Welcome to React Native v0.80
Starting dev server on http://localhost:8081

 ┌────────────────────────────────────────┐
 │                                        │
 │  Metro is now ready.                   │
 │                                        │
 │  To reload the app press "r"           │
 │  To open developer menu press "d"      │
 │                                        │
 └────────────────────────────────────────┘

INFO  Dev server ready.
```

**문제 해결:**
- ❌ `EADDRINUSE: address already in use :::8081` 에러 발생 시:
  ```powershell
  # 1. 기존 프로세스 찾기
  netstat -ano | findstr :8081
  # 출력: TCP 0.0.0.0:8081 ... LISTENING [PID]

  # 2. PID를 메모한 후 프로세스 종료 (예: PID=55660)
  powershell -Command "Stop-Process -Id 55660 -Force"

  # 3. Metro 재시작
  npm start
  ```

**이 터미널은 계속 실행 상태로 유지하세요!**

---

**단계 2: ADB 포트 포워딩 설정 (중요!)**

**터미널 2 (새 터미널 열기):**
```powershell
adb reverse tcp:8081 tcp:8081
```

**정상 출력:**
```
(출력 없음 또는 빈 줄)
```

**❌ 에러 발생 시:**
```
adb: error: no devices/emulators found
```
→ 해결: `adb devices` 실행하여 디바이스 연결 확인

**⚠️ 매우 중요한 사항:**
- **USB 케이블을 뽑았다가 다시 꽂으면 반드시 이 명령 재실행 필요**
- **PC 재부팅 시 반드시 재실행 필요**
- **"Unable to load script" 에러 = 99% 포트 포워딩 안 된 것**

**포트 포워딩 확인:**
```powershell
adb reverse --list
```
**출력:**
```
tcp:8081 tcp:8081
```

---

**단계 3: 앱 빌드 및 설치**

**터미널 2 (계속 사용):**
```powershell
cd C:\Users\tjehd\capstone\LaundryClassifier\android

# JAVA_HOME 설정 (PowerShell)
$env:JAVA_HOME = 'C:\Program Files\Eclipse Adoptium\jdk-17.0.18.8-hotspot'

# 빌드 및 설치
./gradlew app:installDebug
```

**빌드 시간:** 약 30초 ~ 1분

**기대 출력 (마지막 줄):**
```
BUILD SUCCESSFUL in 37s
69 actionable tasks: 59 executed, 10 up-to-date
```

**앱 설치 확인:**
```
Installing APK 'app-debug.apk' on 'SM-A908N - 12'
Installed on 1 device.
```

---

**단계 4: 앱 실행**

**방법 1: 디바이스에서 수동 실행**
- 디바이스 홈 화면에서 "LaundryClassifier" 앱 아이콘 탭

**방법 2: ADB로 실행 (터미널에서)**
```powershell
adb shell am start -n com.anonymous.LaundryClassifier/.MainActivity
```

**성공 시 Metro 터미널에 다음 로그 출력:**
```
INFO  ./index.js ............................ BUNDLE [android, dev] 100.0% (1/1), done.
```

---

### 7.3 빠른 재시작 (코드 수정 후)

JavaScript 파일(`.js`, `.tsx`)만 수정했을 때:

**방법 1: 디바이스에서 리로드**
- 디바이스 흔들기 (Shake) → "Reload" 탭
- 또는 디바이스에서 `RR` (더블탭)

**방법 2: Metro에서 리로드**
- Metro 터미널에서 `r` 키 누르기

**네이티브 코드 변경 시 (`.kt`, `.gradle`, `AndroidManifest.xml`):**
- 반드시 **단계 3 (./gradlew app:installDebug)** 재실행 필요

---

### 7.4 일반적인 문제 해결

#### 문제 1: "Unable to load script from assets 'index.android.bundle'"

**빨간 화면에 이 에러가 나타남**

**원인:**
- 90% 확률: 포트 포워딩 안 됨
- 10% 확률: Metro 번들러 안 켜짐

**해결 순서:**
```powershell
# 1. Metro 번들러 실행 중인지 확인
curl http://localhost:8081/status
# 정상: {"packager-status":"running"}
# 에러: (연결 실패) → npm start 실행

# 2. 포트 포워딩 재설정
adb reverse tcp:8081 tcp:8081

# 3. 앱 재시작
adb shell am start -n com.anonymous.LaundryClassifier/.MainActivity
```

---

#### 문제 2: "Cannot find module 'xxx'" (Metro 에러)

**Metro 터미널에 빨간 에러 메시지**

**원인:**
- `babel.config.js`가 설치 안 된 패키지 참조
- `node_modules` 손상

**해결:**
```powershell
# 1. babel.config.js 확인
cat babel.config.js
# plugins 배열에 없는 패키지가 있으면 제거

# 2. node_modules 재설치
rm -rf node_modules package-lock.json
npm install

# 3. Metro 재시작
npm start
```

---

#### 문제 3: "EADDRINUSE: address already in use :::8081"

**Metro 시작 안 됨**

**원인:** 이전 Metro 프로세스가 살아있음

**해결:**
```powershell
# Windows PowerShell
netstat -ano | findstr :8081
# 출력: ... LISTENING [PID]

# PID 숫자를 확인 후 (예: 55660)
powershell -Command "Stop-Process -Id 55660 -Force"

# Metro 재시작
npm start
```

---

#### 문제 4: "adb: error: no devices/emulators found"

**adb 명령어 실행 시**

**원인:**
- USB 케이블 연결 끊김
- USB 디버깅 비활성화
- ADB 서버 문제

**해결:**
```powershell
# 1. ADB 서버 재시작
adb kill-server
adb start-server

# 2. 디바이스 연결 확인
adb devices

# 3. unauthorized 나오면
# → 디바이스에서 "USB 디버깅 허용" 팝업 다시 허용
# → 필요 시 USB 디버깅 끄고 다시 켜기
```

---

#### 문제 5: 앱 실행 즉시 크래시 (로그 없이 종료)

**원인:**
- 네이티브 라이브러리 누락
- React Native 버전 문제
- MainApplication.kt 초기화 오류

**해결:**
```powershell
# 1. Logcat으로 에러 확인
adb logcat | findstr "AndroidRuntime"

# 2. 주요 에러별 해결
# - "libreact_featureflagsjni.so not found"
#   → React Native 버전 확인 (0.80.1이어야 함)
#   → package.json 수정 후 npm install

# - "Unresolved reference: createReactHost"
#   → MainApplication.kt가 올바른지 확인
#   → KIDURIN 패턴 사용 (getDefaultReactHost)

# 3. 클린 빌드
cd android
./gradlew clean
./gradlew app:installDebug
```

---

### 7.5 완전 초기화 (최후의 수단)

모든 게 안 될 때 사용:

```powershell
cd C:\Users\tjehd\capstone\LaundryClassifier

# 1. Node 캐시 정리
rm -rf node_modules package-lock.json
npm cache clean --force

# 2. Gradle 캐시 정리
cd android
./gradlew clean
rm -rf .gradle app/build

# 3. 재설치
cd ..
npm install

# 4. Metro 캐시 정리
npx react-native start --reset-cache
```

---

### 7.6 개발 팁

#### Metro 단축키
- `r` - 앱 리로드
- `d` - 개발자 메뉴 열기
- `i` - iOS 시뮬레이터 실행 (macOS만)
- `a` - Android 에뮬레이터 실행

#### 디바이스 개발자 메뉴
- 디바이스 흔들기 (Shake)
- 또는 `adb shell input keyevent 82`

**개발자 메뉴 옵션:**
- "Reload" - JS 리로드
- "Debug" - Chrome DevTools 연결
- "Show Inspector" - 요소 검사
- "Enable Hot Reloading" - 자동 리로드 (비권장, 불안정)

#### 효율적인 개발 워크플로우
1. Metro는 **한 번만 켜고 계속 실행 상태 유지**
2. JS 파일 수정 → Metro에서 `r` 키만 누르면 됨
3. 네이티브 파일 수정 시에만 `./gradlew app:installDebug` 재실행
4. USB 재연결 시 **반드시** `adb reverse tcp:8081 tcp:8081` 재실행

---

## 📊 8. 최종 구성 요약

### 8.1 핵심 버전 정보

| 항목 | 버전 | 비고 |
|------|------|------|
| **React Native** | 0.80.1 | KIDURIN 참고 (0.81.5에서 변경) |
| **React** | 19.1.0 | (18.3.1에서 변경) |
| **Node.js** | 20.18.3 | (확인 필요) |
| **JDK** | 17.0.18.8 | Eclipse Adoptium |
| **Gradle** | 8.11.1 | (wrapper) |
| **Android SDK** | 35 (compileSdk) | |
| **Android Target** | 35 | |
| **Android Min** | 24 | (Android 7.0+) |

### 8.2 주요 의존성

**Production:**
- `react-native-image-picker@8.2.1` - 카메라/갤러리 이미지 선택
- `react-native-image-resizer@1.4.5` - 이미지 640x640 리사이징 (YOLO 입력용)
- `react-native-vision-camera@4.7.1` - 고성능 카메라 촬영 (Vision Camera)
- `react-native-worklets-core@1.6.2` - Vision Camera 의존성

**DevDependencies:**
- `@react-native-community/cli@latest` - React Native CLI (autolinking 해결)
- `@react-native-community/cli-platform-android@19.0.0`
- `@react-native/babel-preset@0.80.1` - Babel 설정
- `@react-native/metro-config@0.80.1` - Metro 번들러 설정
- `typescript@5.0.4` - TypeScript 지원

### 8.3 네이티브 모듈 링킹

**settings.gradle:**
```gradle
include ':react-native-image-picker'
project(':react-native-image-picker').projectDir = new File(rootProject.projectDir, '../node_modules/react-native-image-picker/android')
```

**app/build.gradle:**
```gradle
dependencies {
    implementation project(':react-native-image-picker')
}
```

---

## ✅ 작업 완료 체크리스트 (최종)

### Expo 제거 및 React Native CLI 전환
- [x] Expo 의존성 완전 제거
- [x] app.json 최소화
- [x] styles.xml SplashScreen 테마 제거
- [x] MainActivity.kt Expo 코드 제거
- [x] MainApplication.kt Expo 코드 제거

### React Native 버전 문제 해결 ⭐ NEW
- [x] KIDURIN 저장소 클론 및 분석
- [x] React Native 0.81.5 → 0.80.1 다운그레이드
- [x] React 18.3.1 → 19.1.0 업그레이드
- [x] MainApplication.kt 완전 재작성 (KIDURIN 패턴)
- [x] babel.config.js 정리 (reanimated 플러그인 제거)
- [x] @react-native-community/cli 패키지 추가

### 빌드 및 실행 환경 구축
- [x] BuildConfig 필드 설정
- [x] react-native-image-picker 수동 링킹
- [x] Gradle 빌드 성공
- [x] 디바이스 설치 성공 (SM-A908N)
- [x] Metro 번들러 포트 충돌 해결
- [x] ADB 포트 포워딩 설정
- [x] 앱 정상 실행 확인 ✅

### 앱 구조 및 기능
- [x] src/ 폴더 구조 생성
- [x] HomeScreen 구현 (카메라/갤러리)
- [x] ResultScreen 구현 (분석 결과)
- [x] App.tsx 화면 전환 로직
- [x] index.js Entry Point 수정

### 문서화 ⭐ NEW
- [x] 작업로그.md 포괄적 업데이트
- [x] 앱 실행 방법 상세 가이드 작성
- [x] 포트 포워딩 설정 방법 명확히 기술
- [x] 일반적인 문제 해결 방법 문서화

### 향후 작업
- [ ] YOLOv8 모델 통합
- [ ] 실제 이미지 분석 기능 구현
- [ ] 전처리/후처리 로직
- [ ] UI/UX 개선
- [ ] 성능 최적화

---

## 🧠 9. YOLOv8 TFLite 모델 포팅 및 On-Device 추론 구현 (2026-05-10)

### 9.1 작업 개요

**목표:** 학습 완료된 YOLOv8n 모델(`best_int8.tflite`)을 React Native 앱에 포팅하여 On-Device AI 추론 파이프라인 완성

**작업 범위:**
1. Android 네이티브 모듈(Kotlin) 작성 - TFLite 모델 로드 및 추론
2. React Native ↔ Native Bridge 구현
3. 이미지 전처리 서비스 구현
4. 모델 디버그 화면 구현 (bbox 시각화 포함)
5. 카메라 촬영 화면 구현 (Vision Camera)
6. 앱 화면 흐름 재구성

---

### 9.2 모델 파일 배치

**모델:** `best_int8.tflite` (YOLOv8n INT8 양자화 모델)
**배치 경로:** `android/app/src/main/assets/models/best_int8.tflite`

**모델 사양:**
- 입력: `[1, 640, 640, 3]` (NHWC, float32, 0~1 정규화)
- 출력: `[1, 42, 8400]` (42 = 4(bbox xywh) + 38(class scores))
- 클래스 수: 38개 (세탁 기호 종류)
- 양자화: INT8 (모바일 최적화)

**38개 클래스 목록:**

| ID | 클래스명 | 설명 |
|----|---------|------|
| 0 | bleach_any | 모든 표백제 사용 가능 |
| 1 | bleach_oxygen_only | 산소계 표백제만 사용 |
| 2 | do_not_bleach | 표백 불가 |
| 3 | do_not_dry_clean | 드라이클리닝 불가 |
| 4 | do_not_iron | 다림질 불가 |
| 5 | do_not_tumble_dry | 텀블 건조 불가 |
| 6 | do_not_wash | 물세탁 불가 |
| 7 | do_not_wet_clean | 습식 세탁 불가 |
| 8 | drip_flat_dry | 물방울 건조 (평면) |
| 9 | drip_flat_dry_shade | 물방울 건조 (평면, 그늘) |
| 10 | drip_line_dry | 물방울 건조 (줄) |
| 11 | drip_line_dry_shade | 물방울 건조 (줄, 그늘) |
| 12 | dry_clean_hc_mild | 드라이클리닝 탄화수소 약하게 |
| 13 | dry_clean_hc_normal | 드라이클리닝 탄화수소 보통 |
| 14 | dry_clean_perc_mild | 드라이클리닝 퍼클로로에틸렌 약하게 |
| 15 | dry_clean_perc_normal | 드라이클리닝 퍼클로로에틸렌 보통 |
| 16 | flat_dry | 평면 건조 |
| 17 | flat_dry_shade | 평면 건조 (그늘) |
| 18 | iron_110c | 다림질 110°C |
| 19 | iron_150c | 다림질 150°C |
| 20 | iron_200c | 다림질 200°C |
| 21 | line_dry | 줄 건조 |
| 22 | line_dry_shade | 줄 건조 (그늘) |
| 23 | tumble_dry_mild | 텀블 건조 약하게 |
| 24 | tumble_dry_normal | 텀블 건조 보통 |
| 25 | wash_by_hand | 손세탁 |
| 26 | washing_mild_30 | 약하게 세탁 30°C |
| 27 | washing_mild_40 | 약하게 세탁 40°C |
| 28 | washing_mild_60 | 약하게 세탁 60°C |
| 29 | washing_normal_30 | 보통 세탁 30°C |
| 30 | washing_normal_40 | 보통 세탁 40°C |
| 31 | washing_normal_60 | 보통 세탁 60°C |
| 32 | washing_normal_95 | 보통 세탁 95°C |
| 33 | washing_very_mild_30 | 매우 약하게 세탁 30°C |
| 34 | washing_very_mild_40 | 매우 약하게 세탁 40°C |
| 35 | wet_clean_mild | 습식 세탁 약하게 |
| 36 | wet_clean_normal | 습식 세탁 보통 |
| 37 | wet_clean_very_mild | 습식 세탁 매우 약하게 |

---

### 9.3 Android 네이티브 모듈 구현

#### 9.3.1 LaundryYOLOModule.kt (핵심 추론 모듈)

**파일:** `android/app/src/main/java/com/anonymous/LaundryClassifier/LaundryYOLOModule.kt`

**구현 내용:**

1. **모델 로드 (`loadModel`):**
   - `assets/models/best_int8.tflite`를 캐시 디렉토리에 복사 후 로드
   - TensorFlow Lite `InterpreterApi` 사용
   - 멀티스레드 설정 (4스레드)

2. **추론 실행 (`runInference`):**
   - 이미지 파일 로드 (`BitmapFactory.decodeFile`)
   - 640x640 리사이징 (`Bitmap.createScaledBitmap`)
   - float32 텐서 변환 (RGB 0~1 정규화, NHWC 포맷)
   - TFLite 추론 실행
   - 후처리 (confidence 필터링 + NMS)

3. **전처리 (`bitmapToTensor`):**
   - Bitmap → ByteBuffer 변환
   - 픽셀별 R, G, B 채널 분리
   - 0~255 → 0~1 정규화 (`/ 255.0f`)
   - NHWC 포맷 (Height, Width, Channel 순서)

4. **후처리 (`postprocess`):**
   - 출력 형태: `[42, 8400]` (42채널 x 8400 박스)
   - rows 0-3: xywh (center_x, center_y, width, height) - 0~1 정규화됨
   - rows 4-41: 38개 클래스별 신뢰도 점수
   - 각 박스에서 최대 신뢰도 클래스 추출
   - 신뢰도 임계값: 0.25 (`CONF_THRESHOLD`)
   - xywh → x1y1x2y2 좌표 변환 (640px 기준)

5. **NMS (Non-Maximum Suppression, `applyNMS`):**
   - IoU 임계값: 0.45 (`IOU_THRESHOLD`)
   - 신뢰도 기준 내림차순 정렬
   - IoU > 0.45인 중복 박스 제거

6. **결과 반환:**
   - `WritableNativeMap` 형태로 JS에 전달
   - 각 detection: `{x1, y1, x2, y2, confidence, classId, bbox[]}`
   - 메타데이터: `{count, inferenceTime}`

**주요 상수:**

```kotlin
INPUT_SIZE = 640        // YOLO 입력 크기
NUM_BOXES = 8400        // YOLO 출력 박스 수
NUM_CLASSES = 38        // 세탁 기호 클래스 수
OUTPUT_CHANNELS = 42    // 4(bbox) + 38(classes)
CONF_THRESHOLD = 0.25f  // 신뢰도 필터링 임계값
IOU_THRESHOLD = 0.45f   // NMS IoU 임계값
```

---

#### 9.3.2 LaundryYOLOPackage.kt (패키지 등록)

**파일:** `android/app/src/main/java/com/anonymous/LaundryClassifier/LaundryYOLOPackage.kt`

**기능:** `LaundryYOLOModule`을 React Native 패키지로 등록

```kotlin
class LaundryYOLOPackage : ReactPackage {
    override fun createNativeModules(reactContext: ReactApplicationContext): List<NativeModule> {
        return listOf(LaundryYOLOModule(reactContext))
    }
    override fun createViewManagers(...) = emptyList()
}
```

---

#### 9.3.3 MainApplication.kt 변경

**변경 사항:** `getPackages()`에 `LaundryYOLOPackage` 수동 등록

```kotlin
override fun getPackages(): List<ReactPackage> =
    PackageList(this).packages.apply {
        add(LaundryYOLOPackage())  // ✅ 추가됨
    }
```

---

### 9.4 TFLite 의존성 추가

**파일:** `android/app/build.gradle`

**추가된 내용:**

1. **TFLite 라이브러리 의존성:**
```gradle
dependencies {
    implementation 'org.tensorflow:tensorflow-lite:2.14.0'
    implementation 'org.tensorflow:tensorflow-lite-api:2.14.0'
}
```

2. **tflite 파일 압축 방지:**
```gradle
android.aaptOptions {
    noCompress "tflite"
}
```

**설명:**
- `noCompress "tflite"`: APK 빌드 시 `.tflite` 파일이 압축되지 않도록 설정
- 압축하면 모델 로드 시 메모리 매핑이 불가능해져 성능 저하 발생

---

### 9.5 JavaScript Bridge 구현

#### 9.5.1 NativeLaundryYOLO.ts

**파일:** `src/services/NativeLaundryYOLO.ts`

**기능:** React Native ↔ Android Native Module 브릿지

```typescript
import {NativeModules} from 'react-native';
const {LaundryYOLO} = NativeModules;

class NativeLaundryYOLO {
  static async loadModel(): Promise<string>  // 모델 로드
  static async detect(imagePath: string): Promise<InferenceResult>  // 추론 실행
}
```

**핵심 처리:**
- `file://` 프리픽스 자동 제거 (`imagePath.replace('file://', '')`)
- Native Module의 Promise 기반 비동기 호출

**TypeScript 인터페이스:**
```typescript
interface Detection {
  x1, y1, x2, y2: number;  // bbox 좌표 (640px 기준)
  confidence: number;       // 신뢰도 (0~1)
  classId: number;          // 클래스 ID (0~37)
  bbox: number[];           // [x1, y1, x2, y2]
}

interface InferenceResult {
  detections: Detection[];  // 검출된 세탁 기호 목록
  count: number;            // 검출 수
  inferenceTime: number;    // 추론 시간 (ms)
}
```

---

#### 9.5.2 imageProcessingService.ts

**파일:** `src/services/imageProcessingService.ts`

**기능:** 이미지 전처리 (640x640 리사이징)

**사용 라이브러리:** `react-native-image-resizer@1.4.5` (새로 설치됨)

**리사이징 설정:**
- 크기: 640x640 (YOLO 모델 입력 크기)
- 포맷: JPEG
- 품질: 85% (KIDURIN과 동일)
- 모드: `cover` (종횡비 유지, 필요시 크롭)
- EXIF 자동 회전 처리

---

### 9.6 새로운 화면 구현

#### 9.6.1 CameraScreen.tsx (카메라 촬영 화면)

**파일:** `src/screens/CameraScreen.tsx`

**사용 라이브러리:** `react-native-vision-camera@4.7.1` (기존 4.6.3에서 업데이트)

**기능:**
- `react-native-vision-camera` 기반 후면 카메라 제어
- `takeSnapshot` 방식 촬영 (KIDURIN과 동일, 품질 85%)
- 1:1 정사각형 카메라 뷰 (세탁 기호 촬영에 최적화)
- 촬영 가이드라인 UI (좌우 브래킷 형태)
- 카메라 권한 자동 요청 처리
- 촬영 가이드 텍스트 표시

**UI 구성:**
- 상단: 뒤로가기 버튼 (←)
- 중앙: 1:1 카메라 뷰 + 가이드라인 오버레이
- 가이드: "세탁기호를 가이드라인 안에 위치시켜 주세요"
- 하단: 보라색 원형 촬영 버튼

---

#### 9.6.2 ModelDebugScreen.tsx (모델 디버그 화면)

**파일:** `src/screens/ModelDebugScreen.tsx`

**기능:** 이미지 전처리 → 모델 추론 → 결과 시각화를 한 화면에서 처리

**처리 흐름:**
1. 모델 로드 (`NativeLaundryYOLO.loadModel()`)
2. 이미지 640x640 리사이징 (`ImageProcessingService.resizeImageTo640x640()`)
3. YOLO 추론 실행 (`NativeLaundryYOLO.detect()`)
4. 결과 표시 (bbox 오버레이 + 클래스명 + 신뢰도)

**시각화 기능:**
- 이미지 위에 Bounding Box 오버레이 표시
- 10가지 색상 순환 사용 (클래스별 구분)
- bbox 상단에 클래스 ID + 클래스명 라벨 표시
- 전처리 정보 카드 (리사이징 상태, 입력 크기, 파일 크기)
- 검출 결과 카드 (추론 시간, 각 검출 항목의 라벨 및 신뢰도 %)

**하단 버튼:**
- "다시 촬영" - HomeScreen으로 돌아가기
- "결과 확인" - ResultScreen으로 이동 (검출 완료 후 활성화)

---

### 9.7 앱 화면 흐름 재구성

**파일:** `src/App.tsx`

**변경 전 (2026-04-29):**
```
Home → Result (가짜 분석 결과)
```

**변경 후 (2026-05-10):**
```
Home → Camera (촬영) → ModelDebug (추론+시각화) → Result (세탁 가이드)
       ↓
     Gallery (갤러리 선택) → ModelDebug → Result
```

**화면 상태 관리:**
```typescript
type Screen = 'home' | 'camera' | 'modelDebug' | 'result';

// 상태 변수
currentScreen: Screen
selectedImageUri: string     // 원본 이미지 URI
resizedImageUri: string      // 640x640 리사이징된 이미지 URI
```

**화면 전환 로직:**
- `home` → `camera`: "카메라로 촬영" 버튼
- `home` → `modelDebug`: "갤러리에서 선택" 후 이미지 선택 시
- `camera` → `modelDebug`: 사진 촬영 완료 시
- `modelDebug` → `result`: "결과 확인" 버튼
- `modelDebug` → `home`: "다시 촬영" 버튼
- `result` → `home`: "처음으로 돌아가기" 버튼

---

### 9.8 새로 설치된 패키지

**package.json 변경사항:**

| 패키지 | 이전 | 이후 | 용도 |
|--------|------|------|------|
| `react-native-image-resizer` | 없음 | `^1.4.5` | 640x640 리사이징 |
| `react-native-vision-camera` | `^4.6.3` | `^4.7.1` | 카메라 촬영 |
| `react-native-worklets-core` | 없음 | `^1.6.2` | vision-camera 의존성 |

---

### 9.9 폴더 구조 (최종)

```
LaundryClassifier/
├── android/
│   └── app/
│       ├── build.gradle                    # TFLite 의존성, noCompress 설정
│       └── src/main/
│           ├── assets/
│           │   └── models/
│           │       └── best_int8.tflite    # ✅ YOLOv8n INT8 모델
│           ├── java/com/anonymous/LaundryClassifier/
│           │   ├── MainActivity.kt
│           │   ├── MainApplication.kt      # LaundryYOLOPackage 등록
│           │   ├── LaundryYOLOModule.kt     # ✅ TFLite 추론 모듈
│           │   └── LaundryYOLOPackage.kt    # ✅ 패키지 등록
│           └── AndroidManifest.xml          # CAMERA 권한 등
├── src/
│   ├── App.tsx                              # 화면 전환 (4개 화면)
│   ├── screens/
│   │   ├── HomeScreen.tsx                   # 메인 (카메라/갤러리)
│   │   ├── CameraScreen.tsx                 # ✅ Vision Camera 촬영
│   │   ├── ModelDebugScreen.tsx             # ✅ 추론 + bbox 시각화
│   │   └── ResultScreen.tsx                 # 세탁 가이드 결과
│   ├── services/
│   │   ├── NativeLaundryYOLO.ts             # ✅ Native Bridge
│   │   └── imageProcessingService.ts        # ✅ 이미지 전처리
│   ├── components/                          # (비어있음)
│   └── utils/                               # (비어있음)
├── index.js
├── package.json
├── babel.config.js
└── metro.config.js
```

---

### 9.10 On-Device 추론 파이프라인 (완성)

```
[사용자 입력]
    ↓
[HomeScreen] 카메라 촬영 or 갤러리 선택
    ↓
[CameraScreen] Vision Camera → takeSnapshot (품질 85%)
    ↓
[ModelDebugScreen]
    ├── 1. 모델 로드 (best_int8.tflite, 4스레드)
    ├── 2. 이미지 전처리 (640x640, cover mode, JPEG 85%)
    ├── 3. Bitmap → float32 텐서 변환 (RGB 0~1, NHWC)
    ├── 4. TFLite 추론 → [1, 42, 8400] 출력
    ├── 5. 후처리 (conf > 0.25 필터링 + NMS IoU 0.45)
    └── 6. bbox 시각화 + 클래스명/신뢰도 표시
    ↓
[ResultScreen] 세탁 가이드 표시
```

---

### 9.11 주요 변경사항 요약 (2026-04-29 → 2026-05-10)

| 항목 | Before (04-29) | After (05-10) |
|------|---------------|---------------|
| **모델** | 없음 (가짜 결과) | best_int8.tflite (YOLOv8n INT8) |
| **추론** | 3초 시뮬레이션 | 실제 TFLite 추론 |
| **전처리** | 없음 | 640x640 리사이징 + RGB 정규화 |
| **후처리** | 없음 | confidence 필터 + NMS |
| **카메라** | image-picker만 사용 | Vision Camera (1:1 촬영) + image-picker |
| **화면 수** | 3개 (Home/Result/App) | 5개 (+Camera/ModelDebug) |
| **화면 흐름** | Home → Result | Home → Camera → ModelDebug → Result |
| **네이티브 모듈** | 없음 | LaundryYOLOModule (Kotlin) |
| **시각화** | 없음 | bbox 오버레이 + 라벨 표시 |
| **클래스** | 가짜 4개 | 실제 38개 세탁 기호 |

---

### 9.12 빌드 설정 변경 요약

| 파일 | 변경 내용 |
|------|----------|
| `app/build.gradle` | TFLite 2.14.0 의존성 추가, `noCompress "tflite"` 추가 |
| `MainApplication.kt` | `LaundryYOLOPackage()` 등록 |
| `AndroidManifest.xml` | CAMERA, READ_MEDIA_IMAGES, RECORD_AUDIO 권한 |
| `package.json` | image-resizer, vision-camera 업데이트, worklets-core 추가 |

---

## ✅ 작업 완료 체크리스트 (최종 - 2026-05-10)

### Expo 제거 및 React Native CLI 전환 (04-29)
- [x] Expo 의존성 완전 제거
- [x] app.json 최소화
- [x] styles.xml SplashScreen 테마 제거
- [x] MainActivity.kt Expo 코드 제거
- [x] MainApplication.kt Expo 코드 제거

### React Native 버전 문제 해결 (04-29)
- [x] KIDURIN 저장소 클론 및 분석
- [x] React Native 0.81.5 → 0.80.1 다운그레이드
- [x] React 18.3.1 → 19.1.0 업그레이드
- [x] MainApplication.kt 완전 재작성 (KIDURIN 패턴)
- [x] babel.config.js 정리

### 빌드 및 실행 환경 구축 (04-29)
- [x] BuildConfig 필드 설정
- [x] react-native-image-picker 수동 링킹
- [x] Gradle 빌드 성공
- [x] 디바이스 설치 성공 (SM-A908N)
- [x] 앱 정상 실행 확인

### YOLOv8 TFLite 모델 포팅 (05-10) ⭐ NEW
- [x] best_int8.tflite 모델 파일 배치 (assets/models/)
- [x] TFLite 의존성 추가 (tensorflow-lite 2.14.0)
- [x] LaundryYOLOModule.kt 구현 (모델 로드 + 추론 + 후처리)
- [x] LaundryYOLOPackage.kt 구현 (패키지 등록)
- [x] MainApplication.kt에 패키지 등록
- [x] NativeLaundryYOLO.ts JS Bridge 구현
- [x] imageProcessingService.ts 전처리 서비스 구현
- [x] react-native-image-resizer 설치

### 화면 구현 (05-10) ⭐ NEW
- [x] CameraScreen.tsx 구현 (Vision Camera 1:1 촬영)
- [x] ModelDebugScreen.tsx 구현 (추론 + bbox 시각화)
- [x] App.tsx 화면 흐름 재구성 (4개 화면 전환)
- [x] react-native-vision-camera 업데이트 (4.6.3 → 4.7.1)
- [x] react-native-worklets-core 설치

### 결과 화면 구현 (05-10) ⭐ NEW — 세탁 가이드 결과창 v3

**작업자:** 서동호

PDF 설계 문서 4종(세탁기호.pdf, 세탁기호_결과창_UI_목업.pdf, 세탁기호_결과창_사용자용_v3.pdf, 종설UI.pdf)과 Python 콘솔 프로토타입(laundry_guide.py)을 기반으로, 결과 화면을 전면 재구현하였다.

**설계 문서 사양 요약:**
- 38개 세탁 기호 × 3종 텍스트(결과 해석, 추천, 주의) — 사용자 친화 어투 (~해요/~돼요)
- 4단계 상태 체계: 안전(safe, #2d8d6f) / 주의(caution, #d97a2a) / 금지(forbidden, #c8472b) / 전문케어(pro, #2e4a73)
- 메인 멘트 결정 트리 8가지 조건
- 추천 세탁 방법 5단계(세탁기→세제/표백→탈수→건조→다림질)
- 8가지 주의 조합 경고(A~H) — 자동 트리거
- 한국 세탁기/건조기 실제 코스명(삼성·LG 기준) 매핑

**신규 파일:**

| 파일 | 용도 | 라인 수 |
|------|------|---------|
| `src/data/laundrySymbolData.ts` | 38개 기호 단위 서술 데이터 + 코스 매핑 테이블 | ~250 |
| `src/data/laundryLogic.ts` | 조합 로직, 멘트 결정 트리, 5단계 추천, 8가지 경고, 전문 케어 배너 | ~280 |

**수정 파일:**

| 파일 | 변경 내용 |
|------|-----------|
| `src/screens/ResultScreen.tsx` | 전면 재작성 — 2화면 구조(요약→상세) 구현 |
| `src/App.tsx` | detections 상태 추가, ResultScreen에 전달 경로 구현, onRetake 핸들러 추가 |

**화면 구조 (UI 목업 기반):**

1. **화면 1 — 결과 요약 (ResultSummaryScreen)**
   - 메인 멘트 (큰 글씨, 조건별 자동 결정)
   - 날짜 + 기호 N개 인식 표시
   - 상태 뱃지 (금지 N / 주의 N)
   - 서브 메시지 박스 (좌측 색상 강조선)
   - 4칸 카드 그리드 (물세탁 / 표백 / 건조 / 다림질)
   - 전문 케어 배너 (드라이클리닝/웨트클리닝 있을 때만)
   - [다시 찍기] [상세 보기] 버튼

2. **화면 2 — 상세 가이드 (DetailGuideScreen)**
   - 상태별 카운트 뱃지
   - 세탁 항목 카드 리스트 (우선순위 정렬: 금지→주의→안전)
   - 주의 조합 경고 박스 (A~H, 심각도별 색상)
   - 전문 케어 안내 카드
   - 추천 세탁 순서 (1~4단계, 번호 원형 뱃지)
   - [다시 찍기] [홈으로] 버튼

**데이터 흐름:**
```
ModelDebugScreen (YOLO 추론)
  → detections [{label, confidence, classId}, ...]
  → App.tsx (상태 저장)
  → ResultScreen (detections prop 전달)
  → laundryLogic.ts (label → 조합 분석 → UI 데이터 생성)
```

**주요 로직 함수 (laundryLogic.ts):**

| 함수 | 역할 |
|------|------|
| `mainSubBody()` | 메인/서브 멘트 결정 (8가지 조건 트리) |
| `buildSteps()` | 추천 세탁 방법 5단계 생성 |
| `detectWarnings()` | 8가지 주의 조합 경고 감지 |
| `proCareBanners()` | 전문 케어 배너 생성 |
| `sortedForDisplay()` | 우선순위 기반 정렬 |
| `countByStatus()` | 상태별 카운트 |
| `statusToState()` | 상태 → 색상/스타일 매핑 |

- [x] ResultScreen에 실제 세탁 가이드 내용 연동
- [x] 클래스명 → 한글 세탁 방법 매핑 구현
- [x] 결과 요약 화면 (화면 1) 구현
- [x] 상세 가이드 화면 (화면 2) 구현
- [x] 메인/서브 멘트 결정 트리 (8가지)
- [x] 추천 세탁 방법 5단계
- [x] 8가지 주의 조합 경고 (A~H)
- [x] 전문 케어 배너 (드라이클리닝/웨트클리닝)
- [x] App.tsx 데이터 전달 경로 수정

### 향후 작업
- [ ] UI/UX 디자인 개선 (Figma 기반)
- [ ] 추론 성능 측정 및 최적화
- [ ] 다양한 촬영 환경에서의 인식률 테스트
- [ ] 보관함(옷장) 기능 구현
- [ ] 결과 공유 기능 구현

---

**작성자:** 서동호
**마지막 업데이트:** 2026-05-10 (세션 5)
**버전:** v4.0 - 세탁 가이드 결과창 v3 구현 완료
**상태:** ✅ 결과 화면 전면 구현 (38개 기호 가이드 + 조합 로직 + 추천 세탁 순서)
