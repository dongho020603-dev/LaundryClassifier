# 세탁 기호 분류 앱 개발 연구노트

**프로젝트명:** LaundryClassifier — On-Device AI 기반 세탁 기호 분류 앱
**개발자:** 서동호
**개발 기간:** 2026년 4월 6일 ~ 5월 10일

---

## 4월 6일 (월) — 1주차: 개발 환경 구축 및 프로젝트 생성

### 활동 정리
- 앱 개발을 위한 로컬 개발 환경 구축 (Node.js, JDK, Android Studio, SDK)
- React Native 프로젝트 초기 생성 (Expo 프레임워크 기반)
- 프로젝트 초기 구조 확인 및 기본 설정 완료

### 상세 내용

On-Device AI 기반의 세탁 기호 분류 앱을 개발하기 위해 먼저 개발 환경을 구축하였다. Node.js 20.18.3(LTS), Eclipse Adoptium JDK 17.0.18.8, Android Studio Ladybug를 설치하고, Android SDK API 35(Android 15), Build-Tools 35.0.0을 설정하였다. 환경 변수로 JAVA_HOME과 ANDROID_HOME을 등록하여 터미널에서 바로 빌드가 가능하도록 구성하였다.

프로젝트 초기 생성은 Expo 프레임워크를 사용하였다. `npx create-expo-app LaundryClassifier --template blank-typescript` 명령으로 TypeScript 기반의 프로젝트를 생성하였으며, Expo Router(파일 기반 라우팅), expo-camera(CameraView), expo-image-picker 등의 라이브러리를 활용하여 빠르게 프로토타입을 구성할 수 있었다. 이 시점에서의 프로젝트 구조는 `app/` 디렉토리 기반의 Expo Router 방식이었다.

---

## 4월 9일 (수) — 1주차: UI 프로토타이핑 및 프레임워크 전환 결정

### 활동 정리
- Expo Router 기반 초기 화면 프로토타이핑 (메인 화면, 결과 화면)
- Expo 프레임워크의 한계점 분석
- Expo → 순수 React Native CLI로의 전환 결정

### 상세 내용

Expo Router를 사용하여 메인 화면(카메라 촬영/갤러리 선택 버튼)과 결과 화면(선택한 이미지 + 더미 분석 결과 표시)을 프로토타이핑하였다. 그러나 개발 과정에서 여러 한계점이 확인되었다. 첫째, Expo Router의 file-based routing이 단순한 2~3개 화면 앱에는 오버스펙이었다. 둘째, `expo-camera`의 CameraView가 카메라 제어 유연성이 부족하였다. 셋째, 향후 TFLite 네이티브 모듈을 직접 작성하여 연동해야 하는데, Expo의 managed workflow로는 이를 지원하지 않는다는 점이 결정적이었다.

이러한 이유로 Expo에서 순수 React Native CLI 구조로 전환하기로 결정하였다. 전환 사유는 크게 네 가지이다: (1) TFLite 모델 직접 로드를 위한 Kotlin 네이티브 모듈 작성 필요, (2) 레퍼런스 프로젝트와의 구조 통일, (3) `react-native-vision-camera`를 통한 고성능 카메라 제어, (4) Gradle 직접 사용을 통한 빌드 과정 세밀 제어.

---

## 4월 13일 (일) — 2주차: Android 네이티브 코드 생성 및 Expo 제거 착수

### 활동 정리
- `npx expo prebuild`를 통한 Android 네이티브 코드 생성
- 생성된 네이티브 코드의 Expo 의존성 분석
- Expo 의존성 제거 작업 시작 (MainActivity.kt, MainApplication.kt)

### 상세 내용

순수 React Native CLI로 전환하기 위해 먼저 `npx expo prebuild --platform android` 명령으로 Android 네이티브 코드를 생성하였다. 생성된 `android/` 폴더에는 MainActivity.kt, MainApplication.kt, AndroidManifest.xml, build.gradle 등이 포함되어 있었다.

그러나 생성된 코드에는 Expo 의존성이 남아 있었다. MainActivity.kt에는 `expo.modules.splashscreen.SplashScreenManager` import가 존재했고, MainApplication.kt에는 `expo.modules.ReactNativeHostWrapper` import가 있었다. 또한 `getJSMainModuleName()`이 `.expo/.virtual-metro-entry`를 반환하고, `getMainComponentName()`이 `"main"`을 반환하는 등 index.js의 AppRegistry 등록명 `"laundryclassifier"`와 불일치하는 문제가 있었다. AndroidManifest.xml에도 Expo Updates 관련 메타데이터가 존재했다.

이 상태에서는 앱이 정상 실행되지 않으므로, Expo 의존성을 수동으로 제거하는 작업에 착수하였다. MainActivity.kt에서 Expo import를 제거하고 `getMainComponentName()`이 `"laundryclassifier"`를 반환하도록 수정하였으며, MainApplication.kt에서는 `getJSMainModuleName()`이 `"index"`를 직접 반환하도록 변경하였다.

---

## 4월 14일 (월) — 2주차: Expo 완전 제거 및 앱 구조 재구성

### 활동 정리
- AndroidManifest.xml에서 Expo 메타데이터 제거
- app.json 최소화 (Expo 플러그인 설정 제거)
- src/ 폴더 구조 생성 및 코드 재배치
- index.js Entry Point 수정

### 상세 내용

Expo 의존성 제거를 마무리하였다. AndroidManifest.xml에서 `expo.modules.updates.ENABLED` 등의 메타데이터를 삭제하고, app.json은 `name`과 `displayName`만 남기는 최소 구성으로 변경하였다.

앱 구조를 순수 React Native CLI 방식으로 재구성하였다. 기존 `app/` 디렉토리 기반의 Expo Router 구조를 버리고, `src/` 폴더 아래에 `screens/`, `services/`, `components/`, `utils/`를 두는 구조로 전환하였다. index.js의 import 경로도 `./app/index`에서 `./src/App`으로 변경하였다.

화면 구현에 있어서는 조건부 렌더링 방식의 간단한 내비게이션을 선택하였다. Expo Router 같은 라우팅 라이브러리 없이, `currentScreen` 상태 변수를 이용하여 화면 전환을 처리하는 방식이다. 이 단순한 구조가 3~4개 화면 규모의 앱에 적합하다고 판단하였다.

---

## 4월 18일 (금) — 3주차: 라이브러리 설치 및 HomeScreen 구현

### 활동 정리
- `react-native-image-picker` 설치
- HomeScreen.tsx 구현 (카메라 촬영/갤러리 선택 기능)
- App.tsx 화면 전환 로직 구현

### 상세 내용

카메라/갤러리 이미지 선택 기능을 위해 `react-native-image-picker@8.2.1`을 설치하였다. 기존 Expo Camera 대신 이 라이브러리를 선택한 이유는, 순수 React Native CLI 환경에서 안정적으로 동작하며 카메라 촬영과 갤러리 선택을 모두 지원하기 때문이다.

HomeScreen.tsx를 구현하였다. 두 개의 메인 버튼("카메라로 촬영", "갤러리에서 선택")을 배치하고, 이미지 선택 시 `onImageSelected(uri)` 콜백을 호출하여 다음 화면으로 전환되도록 하였다. 에러 핸들링도 포함하여, 사용자가 이미지 선택을 취소하거나 오류가 발생한 경우를 처리하였다.

App.tsx에서는 `useState`를 활용한 화면 전환 로직을 구현하였다. 초기에는 Home → Result의 2화면 구조로 시작하였으며, 이미지 URI를 상태로 관리하여 화면 간 데이터를 전달하는 방식을 채택하였다.

---

## 4월 21일 (월) — 3주차: ResultScreen 초기 구현 및 빌드 오류 해결

### 활동 정리
- ResultScreen.tsx 초기 구현 (플레이스홀더 버전)
- styles.xml SplashScreen 리소스 오류 해결
- MainApplication.kt 컴파일 오류 해결 (New Architecture 관련)
- BuildConfig 필드 추가

### 상세 내용

ResultScreen.tsx의 초기 버전을 구현하였다. 이 시점에서는 아직 실제 AI 모델이 연동되지 않았으므로, 선택한 이미지를 표시하고 "TFLite 모델 연결 후 가이드가 표시됩니다"라는 안내 텍스트를 보여주는 플레이스홀더 화면으로 구성하였다.

이후 Gradle 빌드를 시도하였으나 여러 오류가 발생하였다. 첫 번째로 `styles.xml`에 남아 있던 Expo SplashScreen 테마 정의가 `Theme.SplashScreen not found` 에러를 일으켰다. 해당 스타일 정의를 삭제하고 `./gradlew clean`으로 빌드 캐시를 정리하여 해결하였다.

두 번째로 MainApplication.kt에서 `Unresolved reference: createReactHost` 등의 컴파일 에러가 발생하였다. 이는 React Native의 New Architecture 관련 API가 현재 사용 중인 버전과 호환되지 않기 때문이었다. `reactHost`를 null로 설정하고, SoLoader를 수동으로 초기화하는 방식으로 임시 해결하였다. 또한 `build.gradle`에 `IS_NEW_ARCHITECTURE_ENABLED`과 `IS_HERMES_ENABLED` BuildConfig 필드를 추가하여 컴파일 에러를 해소하였다.

---

## 4월 26일 (토) — 4주차: 수동 링킹 및 빌드 성공

### 활동 정리
- `react-native-image-picker` 수동 링킹 문제 해결
- Gradle 빌드 최종 성공 (app-debug.apk 생성)
- 실제 디바이스(SM-A908N) 설치 및 실행 확인

### 상세 내용

빌드 과정에서 `react-native-image-picker`의 autolinking이 정상 동작하지 않는 문제가 발생하였다. PackageList.java는 자동 생성되었으나, 모듈의 Gradle 프로젝트가 include되지 않아 `com.imagepicker.ImagePickerPackage` 클래스를 찾을 수 없다는 에러가 발생하였다. npm 재설치, 빌드 캐시 삭제 등을 시도하였으나 해결되지 않았고, 최종적으로 수동 링킹 방식으로 해결하였다.

`settings.gradle`에 `include ':react-native-image-picker'`와 프로젝트 디렉토리 경로를 명시하고, `app/build.gradle`의 dependencies에 `implementation project(':react-native-image-picker')`를 추가하였다. Expo에서 CLI로 변환한 프로젝트에서는 autolinking이 완전히 동작하지 않을 수 있으므로, 수동 링킹이 필요한 경우가 있다는 점을 학습하였다.

이후 빌드에 성공하여 `app-debug.apk`를 생성하고, SM-A908N(Galaxy A90 5G, Android 12) 디바이스에 USB 디버깅을 통해 설치하였다. 앱이 정상적으로 실행되고 Metro bundler와 연결되는 것을 확인하였다.

---

## 4월 28일 (월) — 4주차: 앱 크래시 해결 및 React Native 버전 안정화

### 활동 정리
- 앱 실행 즉시 크래시 문제 분석 (`libreact_featureflagsjni.so` 누락)
- React Native 0.80.1로 버전 변경 (React 19.1.0)
- MainApplication.kt 완전 재작성 (표준 초기화 패턴 적용)
- babel.config.js 정리 및 Metro 번들러 문제 해결

### 상세 내용

앱이 디바이스에 설치는 되었으나 실행 즉시 크래시가 발생하는 문제가 있었다. Logcat 분석 결과 `java.lang.UnsatisfiedLinkError: dlopen failed: library "libreact_featureflagsjni.so" not found`라는 에러가 확인되었다. 이는 사용 중이던 React Native 버전이 잘못 설정되어 필수 네이티브 라이브러리가 누락된 것이 원인이었다.

레퍼런스 프로젝트의 GitHub 저장소를 클론하여 분석한 결과, React Native 0.80.1과 React 19.1.0을 사용하고 있음을 확인하였다. 또한 MainApplication.kt의 초기화 패턴이 우리 프로젝트와 크게 달랐다. 레퍼런스 프로젝트에서는 `getDefaultReactHost()`와 `loadReactNative()`를 사용하는 간결한 구조였는데, 우리 프로젝트에서는 `reactHost`를 null로 설정하고 SoLoader를 수동 호출하는 방식이었다.

package.json에서 React Native를 0.80.1로, React를 19.1.0으로 변경하고 `node_modules`를 삭제 후 재설치하였다. MainApplication.kt도 레퍼런스 프로젝트의 패턴을 따라 완전히 재작성하였다. `loadReactNative(this)`가 내부적으로 SoLoader 초기화를 올바르게 처리하고, `getDefaultReactHost()`가 네이티브 라이브러리 로딩을 정상적으로 수행하여 크래시가 해결되었다.

추가로 babel.config.js에서 이미 제거된 `react-native-reanimated` 플러그인 참조를 정리하였고, Metro 번들러의 포트 충돌(EADDRINUSE), ADB 포트 포워딩(`adb reverse tcp:8081 tcp:8081`) 누락 등의 문제도 순차적으로 해결하여 앱이 완전히 정상 동작하는 것을 확인하였다.

---

## 5월 3일 (토) — 5주차: YOLOv8 TFLite 모델 포팅 및 네이티브 모듈 구현

### 활동 정리
- YOLOv8n INT8 양자화 모델(`best_int8.tflite`) Android assets 배치
- Kotlin 네이티브 모듈 작성 (LaundryYOLOModule.kt — 모델 로드, 추론, 후처리)
- TFLite 의존성 추가 (tensorflow-lite 2.14.0)
- React Native ↔ Native Bridge 구현 (NativeLaundryYOLO.ts)

### 상세 내용

학습 완료된 YOLOv8n 모델의 INT8 양자화 버전(`best_int8.tflite`)을 `android/app/src/main/assets/models/` 경로에 배치하였다. 이 모델은 입력 `[1, 640, 640, 3]`(NHWC, float32, 0~1 정규화), 출력 `[1, 42, 8400]`(4개 bbox + 38개 클래스 점수 × 8400 후보 박스)의 사양을 가진다.

Kotlin으로 핵심 추론 네이티브 모듈 `LaundryYOLOModule.kt`를 작성하였다. 이 모듈은 TensorFlow Lite InterpreterApi를 사용하여 모델을 로드하고(4스레드 멀티스레드 설정), 입력 이미지를 640×640으로 리사이징한 뒤 float32 텐서로 변환(RGB 0~1 정규화, NHWC 포맷)하여 추론을 실행한다. 후처리 단계에서는 출력의 `[42, 8400]` 형태를 파싱하여 각 박스에서 최대 신뢰도 클래스를 추출하고, 신뢰도 임계값 0.25로 필터링한 뒤 NMS(Non-Maximum Suppression, IoU 임계값 0.45)를 적용하여 중복 박스를 제거한다.

`LaundryYOLOPackage.kt`를 작성하여 네이티브 모듈을 React Native 패키지로 등록하고, MainApplication.kt의 `getPackages()`에 추가하였다. JavaScript 측에서는 `NativeLaundryYOLO.ts` Bridge를 통해 `loadModel()`과 `detect(imagePath)` 메서드를 Promise 기반으로 호출할 수 있도록 하였다.

`app/build.gradle`에 TFLite 라이브러리 의존성(`tensorflow-lite 2.14.0`)을 추가하고, `.tflite` 파일이 APK 빌드 시 압축되지 않도록 `noCompress "tflite"` 설정을 적용하였다.

---

## 5월 5일 (월) — 5주차: 카메라 화면 및 모델 디버그 화면 구현

### 활동 정리
- CameraScreen.tsx 구현 (Vision Camera 기반 1:1 촬영)
- ModelDebugScreen.tsx 구현 (이미지 전처리 + YOLO 추론 + bbox 시각화)
- 이미지 전처리 서비스 구현 (imageProcessingService.ts)
- `react-native-image-resizer`, `react-native-worklets-core` 설치

### 상세 내용

카메라 촬영 화면 `CameraScreen.tsx`를 `react-native-vision-camera@4.7.1` 기반으로 구현하였다. 세탁 기호 촬영에 최적화된 1:1 정사각형 카메라 뷰를 제공하며, `takeSnapshot` 방식(품질 85%)으로 촬영한다. 촬영 가이드라인 UI(좌우 브래킷 형태)와 안내 텍스트("세탁기호를 가이드라인 안에 위치시켜 주세요")를 오버레이로 표시하여 사용자가 올바른 위치에서 촬영할 수 있도록 유도하였다. 카메라 권한 자동 요청 처리도 포함하였다.

모델 디버그 화면 `ModelDebugScreen.tsx`는 이미지 전처리부터 모델 추론, 결과 시각화까지의 전체 파이프라인을 한 화면에서 처리한다. 처리 흐름은 다음과 같다: (1) 모델 로드, (2) 이미지 640×640 리사이징, (3) YOLO 추론 실행, (4) 결과 표시. 이미지 위에 Bounding Box 오버레이를 표시하며, 10가지 색상을 순환 사용하여 클래스별 구분이 가능하도록 하였다. 전처리 정보 카드(리사이징 상태, 입력 크기, 파일 크기)와 검출 결과 카드(추론 시간, 각 항목의 라벨 및 신뢰도)도 함께 표시한다.

이미지 전처리를 위해 `react-native-image-resizer@1.4.5`를 새로 설치하였다. 640×640 cover 모드 리사이징, JPEG 85% 품질, EXIF 자동 회전 처리를 수행한다. Vision Camera의 의존성인 `react-native-worklets-core@1.6.2`도 함께 설치하였다.

---

## 5월 9일 (금) — 6주차: 앱 화면 흐름 재구성 및 빌드 테스트

### 활동 정리
- App.tsx 화면 흐름 4개 화면으로 재구성 (Home → Camera → ModelDebug → Result)
- detections 데이터 전달 경로 구현
- 네이티브 코드 변경에 따른 Gradle 재빌드 및 디바이스 테스트
- 실제 디바이스에서 전체 파이프라인 동작 검증

### 상세 내용

App.tsx의 화면 흐름을 기존 Home → Result의 2화면 구조에서, Home → Camera → ModelDebug → Result의 4화면 구조로 재구성하였다. 화면 전환은 `currentScreen` 상태 변수를 이용하며, 상태 관리에 `selectedImageUri`(원본 이미지), `resizedImageUri`(640×640 리사이징 이미지), `detections`(YOLO 추론 결과) 세 가지를 추가하였다.

갤러리에서 이미지를 선택한 경우에는 Camera 화면을 건너뛰고 바로 ModelDebug 화면으로 이동하도록 처리하였다. ModelDebugScreen에서 "결과 확인" 버튼을 누르면 검출된 detections 배열이 App.tsx의 상태에 저장되고, ResultScreen으로 전달된다.

LaundryYOLOModule.kt 등 네이티브 코드가 변경되었으므로 `./gradlew app:installDebug`로 재빌드하여 SM-A908N 디바이스에 설치하였다. 빌드 성공 후 실제 세탁 라벨을 촬영하여 전체 파이프라인(카메라 촬영 → 640×640 전처리 → TFLite 추론 → NMS 후처리 → bbox 시각화)이 정상적으로 동작하는 것을 검증하였다. 추론 시간, 검출된 세탁 기호 수, 신뢰도 등의 정보가 ModelDebugScreen에서 정확히 표시되었다.

---

## 5월 10일 (토) — 6주차: 세탁 가이드 결과 화면 전면 구현

### 활동 정리
- PDF 설계 문서 4종 분석 (세탁기호.pdf, UI 목업, 사용자용 v3, 종설 UI)
- Python 콘솔 프로토타입(laundry_guide.py)의 TypeScript 포팅
- 세탁 기호 데이터 모듈 작성 (laundrySymbolData.ts — 38개 기호 데이터)
- 세탁 가이드 조합 로직 모듈 작성 (laundryLogic.ts)
- ResultScreen.tsx 전면 재작성 (2화면 구조: 요약 ↔ 상세)

### 상세 내용

결과 화면을 본격적으로 구현하기 위해 먼저 4종의 PDF 설계 문서를 분석하였다. 세탁기호.pdf(18페이지)에서 38개 기호의 분류 체계와 상태 정의(안전/주의/금지/전문케어), 메인 멘트 결정 조건 8가지, 8가지 주의 조합 경고(A~H)를 파악하였다. 세탁기호_결과창_UI_목업.pdf(4페이지)에서 2화면 구조의 UI 레이아웃과 디자인 토큰(색상 코드, 폰트 크기, 간격 등)을 확인하였다. 세탁기호_결과창_사용자용_v3.pdf(33페이지)에서는 38개 기호 각각에 대한 사용자 친화적 텍스트(결과 해석, 추천 방법, 주의사항)를 확인하였다.

이 설계 문서들의 사양이 이미 Python 콘솔 프로토타입(`laundry_guide.py`, 1047줄)으로 구현되어 있었으므로, 이를 TypeScript로 포팅하는 방식을 택하였다.

`src/data/laundrySymbolData.ts`에 38개 세탁 기호의 단위 데이터를 정의하였다. 각 기호는 카테고리(물세탁/표백/건조/다림질/전문케어), 상태(안전/주의/금지/전문케어), 카드 텍스트, 값 텍스트, 결과 해석, 추천 방법, 주의사항을 포함한다. 한국 세탁기/건조기의 실제 코스명(삼성/LG 기준)을 매핑하는 WASH_COURSE 테이블도 함께 정의하였다.

`src/data/laundryLogic.ts`에 조합 로직 함수들을 구현하였다. `mainSubBody()` 함수는 8가지 조건에 따라 메인 멘트와 서브 메시지를 결정하는 결정 트리이다. `buildSteps()` 함수는 감지된 기호 조합에 따라 5단계 추천 세탁 방법(세탁기 → 세제/표백 → 탈수 → 건조 → 다림질)을 생성한다. `detectWarnings()` 함수는 특정 기호 조합이 감지되면 A~H 8가지 주의 경고를 자동으로 트리거한다. `proCareBanners()` 함수는 드라이클리닝이나 웨트클리닝 기호가 있을 때 전문 케어 배너를 생성한다.

`ResultScreen.tsx`를 전면 재작성하여 2화면 구조로 구현하였다. 화면 1(결과 요약)에서는 메인 멘트, 날짜 정보, 상태 뱃지, 서브 메시지 박스, 4칸 카테고리 카드 그리드(물세탁/표백/건조/다림질), 전문 케어 배너를 표시한다. 화면 2(상세 가이드)에서는 상태별 카운트 뱃지, 세탁 항목 카드 리스트(우선순위 정렬), 주의 조합 경고 박스, 전문 케어 안내 카드, 추천 세탁 순서를 표시한다.

App.tsx에서 `detections` 상태를 추가하여 ModelDebugScreen에서 ResultScreen으로 검출 결과를 전달하는 경로를 구현하였다. ResultScreen은 전달받은 detections를 `laundryLogic.ts`의 함수들로 분석하여 모든 UI 데이터를 `useMemo`로 계산한다.

이로써 On-Device AI 기반 세탁 기호 분류 앱의 전체 파이프라인이 완성되었다. 카메라 촬영 또는 갤러리 선택 → 이미지 전처리 → YOLOv8 TFLite 추론 → 후처리 → 세탁 가이드 결과 표시까지의 일련의 과정이 실제 디바이스에서 정상적으로 동작한다.

---

## 개발 결과 요약

### 최종 기술 스택

| 항목 | 사양 |
|------|------|
| 프레임워크 | React Native 0.80.1 (CLI) |
| 언어 | TypeScript (프론트엔드), Kotlin (네이티브 모듈) |
| AI 모델 | YOLOv8n INT8 양자화 (best_int8.tflite) |
| 추론 엔진 | TensorFlow Lite 2.14.0 (On-Device) |
| 카메라 | react-native-vision-camera 4.7.1 |
| 대상 기기 | Android 7.0+ (API 24+) |

### 앱 화면 구성

```
HomeScreen → CameraScreen → ModelDebugScreen → ResultScreen
(메인)       (촬영)         (추론+시각화)      (세탁 가이드)
```

### 핵심 구현 사항
- 38개 세탁 기호 분류 (5개 카테고리: 물세탁, 표백, 건조, 다림질, 전문케어)
- 4단계 상태 체계 (안전/주의/금지/전문케어)
- 메인 멘트 결정 트리 (8가지 조건)
- 추천 세탁 방법 5단계 자동 생성
- 8가지 주의 조합 경고 자동 감지
- 전문 케어 배너 자동 생성

---

**작성자:** 서동호
**작성일:** 2026년 5월 10일
